# Nesting of mechatronic components

Generally, there are recurring configurations in a project. To minimize the configuration work, closed units can be formed. These consist of a component in which further components are installed (pre-initiated). By means of Disablers the installed components can be deactivated, depending on the type of the project.

Mechatronical components have the Components field below the editor menu.

With ![](../Pictures/Gui/ALL/new.png) (Add component) a component, a placeholder or an extension point is added.

With ![](../Pictures/Gui/duplicate_component.png) (Duplicate component) a selected installed component is duplicated.

With ![](../Pictures/Gui/replace_component.png) (Replace component) a selected installed component is replaced with another component.

The following figure shows that in the mechatronic component Axis the components Cylinder, Valve, Position\_1, Position\_2, Position\_3, and Position\_4 are installed (pre-initiated).

![](../Pictures/verschachteln.png)

If you install this Axis component in accordance with the figure above, this results in the following structure:

![](../Pictures/projekt_explorer_eingebaute_komponenten.png)