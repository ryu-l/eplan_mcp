# ENI folder for created projects

Path to model variable:

Disciplines > PLC > CoDeSys > Code Generator

Specify a string value. The value determines the name of a folder in which the created projects are stored.