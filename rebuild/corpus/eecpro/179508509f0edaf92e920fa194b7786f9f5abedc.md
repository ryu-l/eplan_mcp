# TEXT

The TEXT discipline serves for the creation of any text file, consisting of folders, template files, files and fragments. The text files can contain parameter sequences that can be replaced during the generation of the documents through current/calculated parameter values.