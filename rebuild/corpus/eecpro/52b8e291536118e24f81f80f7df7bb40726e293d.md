# Create parameter units on demand

Path to model variable:

Disciplines > Office > Word

Select a Boolean value. The value determines whether missing parameter units are created automatically when Word resources are added.

Valid values are true and false.