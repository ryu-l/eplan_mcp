# Engineering.StartFormulaCacheCommand

Activates the formula cache. With activated formula cache all the results of the calculations are stored and are faster available in re-calculations. Objective data (e.g. wiring diagrams, program code, etc.) can thus be created faster.

No arguments are passed for this command.