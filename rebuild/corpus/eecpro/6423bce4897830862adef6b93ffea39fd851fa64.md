# Print function for diagrams

It is now possible to print graph-UIs. The printout can be optimized using the âprint previewâ function. For example, the diagram can be scaled to fit a single page.