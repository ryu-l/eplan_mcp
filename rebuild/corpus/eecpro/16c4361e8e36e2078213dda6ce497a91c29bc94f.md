# Parameter assignment via Add parameter button

Using the Add parameter button ![](../Pictures/button_finder.png), the Parameter Finder dialog is started:

![](../Pictures/finder_parameter.png)

In the Parameter Finder dialog, the desired parameter can be selected. This will assign it to the mechatronic component.