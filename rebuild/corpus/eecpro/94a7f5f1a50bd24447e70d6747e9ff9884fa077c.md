# Toolbars

The EEC Workbench contains a global toolbar **(A)** which is used to execute global actions, for example, full-text search, and a view-specific toolbar (B), for example, to generate the schematic for the project displayed in a view.

![](../Pictures/eecbase_Toolbar.png)