# <xsl:template>

The tag <xls:template> describes templates that are constructed on nodes of the original tree (XML created by third-party supplier) in order to translate from the original tree to a target tree (format that can be interpreted by EEC).