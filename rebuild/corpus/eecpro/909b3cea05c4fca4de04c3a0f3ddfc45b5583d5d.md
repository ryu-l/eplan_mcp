# Decorators

Path to the preferences:

Menu Window > Preferences

Dialog Preferences > General > Editors > Decorators

For objects abstraction layers can be activated/deactivated as additional decorators via the elements or symbols.