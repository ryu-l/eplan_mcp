# Modeling controller components

The IO generator writes the created addresses to the parameters for the input start-address or output start-address. The parameters that are required for parameterization of the IO generator are described in the following subsections.

![](../Pictures/2008030502.svg)