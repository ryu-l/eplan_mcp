# Preferences for Project Explorer

Path to the preferences:

Menu Window > Preferences

Dialog Preferences > Unit Explorer > Project Explorer

- Close all editors when different project is selected in project explorer closes all editors if another project is selected. To enable comparision of projects, this option must be unchecked.