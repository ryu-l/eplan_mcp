# Part 2 â Placing components on placeholders and extension points

The import wizard cannot only be used to create a new configuration. In an existing configuration individual components can also be placed on placeholders and extension points.