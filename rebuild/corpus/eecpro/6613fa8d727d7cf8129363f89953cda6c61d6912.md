# Local variables

In formulas local variables are defined by means of the keyword let. These are to be used instead of the expressions which they represent. This improves the overview for complex formulas.