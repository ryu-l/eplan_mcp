# de.eplan.eec.jobserver.port

The EEC argument must be transferred with the following syntax:

```
-D<Argument>=<Value>
```

| EEC argument | Usage |
| --- | --- |
| -Dde.eplan.eec.jobserver.port=<Port number> | Only for Job Server |
| **Annotation** | |
| Number of the port on which the Job Server is listening.  This specification is only valid for connections using the http protocol.  Example:   ``` -Dde.eplan.eec.jobserver.port=8686 ``` | |