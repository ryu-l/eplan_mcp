# Prefix for created project labels

Path to model variable:

Disciplines > PLC > CoDeSys > Code Generator

Specify a string value. The value determines the prefix that precedes created project labels.