# Tab Conduit / line

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Dialog call:](javascript:void(0);)

You have opened a project.

- You have selected a conduit definition line or the symbol of a conduit, piping or hose line definition in the graphical editor. Popup menu item Properties. In the Properties <...> dialog, select the Conduit / line tab.
- You have selected a conduit definition in the device navigator. Popup menu item Properties or Properties (global). In the Properties <...> dialog, select the Conduit / line tab.

Use this tab to edit the properties of a conduit, a piping or hose line.

In the Properties (global) processing mode, you edit the properties of all representations of the function jointly; in this case, the tab shows the supplement "(Device)".

Overview of the main dialog elements:

Displayed DT:

The displayed DT is displayed in the graphical editor on the component. If you have defined in the project settings that devices are to be automatically numbered when symbols are inserted, then a DT is automatically suggested here. You can accept this DT or overwrite it manually.

In addition you can use the [popup menu or the [...] button](devicetaggui_r_feldsichtbaresbmk.htm) to create or select a DT.

Full DT:

This field displays the full DT (i.e. structure identifier of the current environment + displayed DT) of the component / item. Use the [...] button to open the Full DT dialog. You can edit the structure identifiers there and select, e.g., another mounting location.

Type:

You can enter a conduit or line type in this field. Click [...] to open the Part selection dialog. There, you can select a part and assign its technical data to the current conduit or the current line.

The parts data is updated â just like with a new part selection. In contrast to a regular part selection, however, no conflict dialog is displayed here with the old and new data, but the old data is overwritten without any further prompt.

Function text:

Here you enter the function text for the function / device.

Aside from the manually entered function text, there are other (automatically determined) function texts that you will find in the table of properties:

- Function text (automatic): Displays the content of the manually entered function text or, if it is empty, the path function text. If you edit this function text in the Properties (global) processing mode, the content is returned to the function where the function text was entered originally. When this property is displayed on a project page, the line breaks are removed.
- Function text (common): Displays the function text (automatic) of the function or, if it is empty, the function text (automatic) of the associated main function.

Number of connections / tubes:

Enter here the number of connections and / or tubes that are contained in the conduit. In a part selection, this value is applied from the parts management, and can be used as a filter criterion in the device selection. In the case of an individual line, for example a hose line, leave the field empty.

Length:

Enter here the length of the conduit or the line (including the unit). If you do not enter a unit, the unit specified in the project settings will be used. An overview of the possible units can be found in the section "[Connections: Units for Lengths and Cross-sections](connectionbrowsergui_k_einheitenverbindungen.htm)".

![](../Pictures/Gui/ALL/note.png)Note:

In addition to the Length property a further property called Hose line length exists that you can have displayed in the table of the properties. This property is filled with data when you import the hose line data which contain the hose line length from the Eplan Hose Line Configurator.

Cross-section / diameter:  
Unit:

The connection cross-section or diameter is adopted from the parts management and can be used as filter criteria when selecting devices. Click [...] to open the [Cross-sections / diameters](connectionsettingsgui_d_einstellungenquerschnitt.htm) dialog and select a cross-section / diameter (with the associated unit) from the list.

If you selected a predefined cross-section / diameter from the Cross-section / diameter field, the associated unit is automatically transferred to the Unit field. Use the drop-down list to select another unit. An overview of the possible units can be found in the section "[Connections: Units for Lengths and Cross-sections](connectionbrowsergui_k_einheitenverbindungen.htm)".

Main function:

Select this check box if the current function is a main function.   
When using a DT for which there is already a main function, when using a placed main function, or when placing an already placed main function, this property will automatically be switched off at the target function, that is, the check box is deactivated.

![](../Pictures/Gui/ALL/note.png)Note:

The setting is automatically compared to the check box of the same name in the Symbol / function data tab.

---

[Properties group box](devicetaggui_r_gruppenfeldeigenschaften.htm)

See also

[Eplan Fluid: Definition of Conduits and Lines](ftechnic_k_schlauchpakete.htm)

[Inserting Conduits, Piping and Hose Lines](ftechnic_h_paketedefinieren.htm)

[Dialog Full DT](devicetaggui_d_vollstaendigesbmk.htm)

[Defining and Formatting Device Tags](devicetaggui_h_bmkfestlegenundformatieren.htm)

[Dialog Settings: Numbering](numerationgui_d_einstellnummerierung.htm)