# P020031: <x>: <y> -> <z>

### Description

You have run a project comparison, checking the Properties of the test project (incoming project) against those of the comparison project and differences were found.

This message occurs when a property has changed in the project to be checked. The name is output with <x>, the previous value with <y> and the new value of the property with <z>.

![](../Pictures/Gui/ALL/note.png)Note:

Using a new check run (any desired scheme) this module-specific message can be deleted from the message management dialog.