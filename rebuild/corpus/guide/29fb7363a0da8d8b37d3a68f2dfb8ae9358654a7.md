# Tab Parts

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Dialog call:](javascript:void(0);)

You have opened a project.

- You have inserted a symbol in the graphical editor or generated a new function in a navigator. You have activated the Main function check box on the <Function category> tab or Symbol / function data tab in the Properties <...> dialog. Select the Parts tab.
- You have selected a general device in the graphical editor or in a navigator. Popup menu item Properties. You have activated the Main function check box on the <Function category> tab or Symbol / function data tab in the Properties <...> dialog. Select the Parts tab.

In this tab, enter or change the part-specific data for the device. Parts can only be added to the Main function, so here you must enter the parts for all functions of the device.

Overview of the main dialog elements:

The parts assigned to the device are listed in the table in the left-hand section of the dialog. Click [...] in the Part number field to open the Part selection dialog. After the selection of a part, the value in the Number of units / quantity column is preset to "1"; you can, however, overwrite this value.

![](../Pictures/Gui/ALL/note.png)Note:

If you selected the Modification allowed during selection check box in the Settings: Part selection dialog (via File > Settings > User > Management > Part selection, for example), existing parts in the parts management module may be modified and new parts created during part selection.   
If the check box is not selected, parts can only be selected. They cannot be created or edited.

Toolbar:

Using the arrow buttons of the toolbars, you can modify the row position of one or several selected parts. This affects, for example, the sequence in which the parts are output in reports. Note that parts can be moved only to free rows. It is always the complete part with all part reference data that is moved. The buttons are only active if it is possible to move the selected parts.

Observe the following when deleting: The parts data are removed from the project. Manually-entered data will be irretrievably lost, but the data entered using the part selection can be reinserted by reselecting the part.

Popup menu:

The popup menu provides - depending on the field type (e.g. date, integer, multilingual) - the following menu items that are, depending on the situation, available for influencing the table or editing the values in the fields. You can find an overview of these popup menu items in the section [Popup menu items](userinterface_m_kontextmenu.htm).

In addition, the following dialog-specific popup menu items are available:

| Menu item | Meaning |
| --- | --- |
| Open hyperlink | Allows opening of external documents or an image file, as long as this is assigned to the part in the parts management. |
| Assign data | Provides the option to transfer the data of a part entered further below to the function. |
| Configure representation | Opens the Configure representation dialog in which you specify the part and part reference properties to be displayed in the table in the left-hand section. properties that were selected for the representation are no longer displayed in the Properties table in the right-hand section (parts data and part reference data). |

Category:

From the drop-down list, select the category whose properties should be displayed in the table below:

- Parts data: The data stored in the project, such as part number and type number. These values cannot be changed here and are therefore displayed grayed out.
- Part reference data: The data copied to the device. Most of them can be edited manually, for example the service time. Values of part reference data that contain a value deviating from the part property are displayed with a yellow cell background. This applies both to the properties table of the left-hand section as well as the properties table of the right-hand section.  
  Please note that, if you repeat the part selection, the reference data will be deleted if it is the same as the part data.
- Parts data: <Scheme name>: The parts data of a property configuration that was created as a scheme in the Properties tab of the parts management with the scheme name displayed here. These values cannot be changed here and are therefore displayed grayed out.

Find:

Into this field enter the text for which the properties are to be filtered. Both the names of the displayed properties as well as the values are searched.

If you want to return to the original view of the properties, remove the search term from the entry field. Click ![](../Pictures/Gui/ALL/all_delete_input_as.png) (Delete) to do so.

Property / Value:

Under the Part reference data category, you can determine whether the assigned part is the main part or an accessory by using the property Part allocation:

- Main part: Identifies the actual main part, e.g. a cable. This value is preset by default for each part.
- Accessory: Identifies an accessory.
- Part source / part target: Identifies parts that have been assigned to a cable as the source or target, e.g. cable terminal end or plug.

This property can also be used in reports and is taken into account when cables are automatically selected.

In the Part reference data category you can use the Part is included in a module property to determine that a certain parts is evaluated as a module part. When outputting the parts list, such parts are suppressed.

Data source:

Select a scheme from the drop-down list which determines the user-specific default settings for part selection. Click [...] to open the [Settings: Parts (user)](partselectiongui_d_datenauswahlbenutzer.htm) dialog, in which you can change these default settings.

[Settings]:

| Menu item | Meaning |
| --- | --- |
| Device selection | Opens the [Settings: Device selection](partselectiongui_d_einstellungengeraeteauswahl.htm) dialog, where you can determine the default settings for device selection. |
| Part selection (project) | Opens the [Settings: Part selection (project)](partselectiongui_d_datenauswahlprojekt.htm) dialog, where you can determine the default settings for part selection. |

[Device selection]:

Click this button to open the [Device selection](partselectiongui_d_geraeteauswahl.htm) dialog to open and select a device.

See also

[Selecting Parts](partselectiongui_h_artikelauswahl.htm)

[Selecting Devices](partselectiongui_h_geraeteauswahl.htm)

[Assigning a Part to a Device](devicetaggui_h_artikeldatenzuweisen.htm)

[Saving Parts Reference Data to Function](partselectiongui_h_artikeldatenanschaltzeichen.htm)