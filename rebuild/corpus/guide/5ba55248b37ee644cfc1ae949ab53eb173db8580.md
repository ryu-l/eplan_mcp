# Dialog Format: Calculation

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Dialog call (form and plot frame editor):](javascript:void(0);)

You have opened a project. Tab Master data > Command group Plot frames / forms > Drop-down button Form > [Open].

- Tab Insert > Command group Text > Drop-down button Forms > Placeholder text. Select the Placement tab in the subsequent dialog and, next to the Properties formatted / calculated field, click [...]. Select the Calculation format element and click ![](../Pictures/Gui/ALL/all_arrowright_as.png) (Move to the right).
- You have double-clicked a placeholder text (or have selected the Properties popup menu item. In this case several texts can also be marked). Select the Placement tab in the subsequent dialog and, next to the Properties formatted / calculated field, click [...]. Select the Calculation format element and click ![](../Pictures/Gui/ALL/all_arrowright_as.png) (Move to the right).

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Dialog call (user-defined properties):](javascript:void(0);)

You have opened a project. Tab Tools > Command group Management > Properties. You have created a user-defined property with the field type "Monolingual text" or "Value with unit" in the Configure properties dialog. You have selected the "Block property" entry in the Display type field and clicked the [...] button in the Block format field. Select the Calculation format element and click ![](../Pictures/Gui/ALL/all_arrowright_as.png) (Move to the right).

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Dialog call (Formats for block properties):](javascript:void(0);)

You have opened a project. You have opened the properties dialog for a function, a page, a symbol, or a project. You have selected the Block property: Format [n] property in the Properties field / Property name field / Properties tab and have clicked [...] in the Value column. Select the Calculation format element and click ![](../Pictures/Gui/ALL/all_arrowright_as.png) (Move to the right).

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Dialog call (display format for segment definitions):](javascript:void(0);)

You have opened a project. Tab Pre-planning > Command group Edit > Configure segment definitions. Click [...] in the Display format navigator field. Select the Calculation format element and click ![](../Pictures/Gui/ALL/all_arrowright_as.png) (Move to the right).

In this dialog you can use format elements to compile a calculation formula. To this purpose project data as well as the parts data of the parts stored in the project are available in the form and plot frame editor. During the configuration of user-defined block properties only project data is offered since these properties are stored project-specifically. User-defined properties for the parts management or for part references cannot be defined as block properties. The data determined via the calculation formula can be displayed in the project or output in reports.

Overview of the main dialog elements:

Available format elements:

This field shows all format elements that can be part of a calculation formula. To load a format element for the calculation formula into the Selected format elements field, select it and then click ![](../Pictures/Gui/ALL/all_arrowright_as.png) ( Move to the right).

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Available format elements in the form and plot frame editor](javascript:void(0);)

The following format elements are available:

| Format element | Meaning |
| --- | --- |
| Placeholder text | Opens the [Placeholder texts - <Form type>](formeditorgui_d_platzhaltertexteauswahl.htm) dialog. Allows insertion of placeholder text into the calculation formula. |
| + | Adds |
| - | Subtracts |
| \* | Multiplies |
| / | Divides |
| ( | Groups several format elements together with the format element ")". This defines the priority of calculation in the calculation formula. |
| ) | Groups several format elements together with the format element "(". This defines the priority of calculation in the calculation formula. |
| Number | Opens the dialog Format: Number. Allows entry of a constant number into the calculation formula. |

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

You wish to automatically calculate and output the total price of parts, including a discount:  
Total purchase price \* (100 - discount) : 100

You have already entered the discount percentage in the parts management in the tab Properties in the property Discount (command path: Tab Master data > Command group Parts > Management). Select the Calculation format element in the dialog Format: Property. Enter the following format elements in the specified sequence in the dialog Format: Calculation:

| Format element | Meaning |
| --- | --- |
| Placeholder text | Select the Placeholder text format element and in the subsequent dialog select the Part reference data placeholder element with the Total purchase price Currency 1 property. |
| \* | Select the "\*" format element. |
| ( | Select the "(" format element. |
| Number | Select the Number format element. Enter a value of "100". |
| - | Select the "-" format element. |
| Placeholder text | Select the Parts data format element and the Discount property. |
| ) | Select the ")" format element. |
| / | Select the "/" format element. |
| Number | Select the Number format element. Enter a value of "100". |

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)](javascript:void(0);)[Available format elements in the user-defined properties / block properties](javascript:void(0);)

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Basic arithmetic operations:](javascript:void(0);)

| Format element | Meaning |
| --- | --- |
| + | Adds |
| - | Subtracts |
| \* | Multiplies |
| / | Divides |
| ^ | Exponentiated |
| ( | Groups several format elements together with the format element ")". This defines the priority of calculation in the calculation formula. |
| ) | Groups several format elements together with the format element "(". This defines the priority of calculation in the calculation formula. |

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Mathematical functions:](javascript:void(0);)

| Format element | Meaning |
| --- | --- |
| abs | Absolute value |
| acos | Arccosine |
| asin | Arcsine |
| atan | Arctangent |
| ceil | Ceiling, returns the smallest integer greater than or equal to the specified decimal number. |
| cos | Cosine |
| cosh | Hyperbolic cosine |
| exp | Exponential function |
| floor | Floor, rounding function, returns the largest integer less than or equal to the specified decimal number. |
| frac | Fractional part outputs the decimal places of a decimal number. If the decimal number is negative, the preceding sign is output as well. For example, the value "0.55" is output for the positive decimal number "1.55" and the value "-0.55" for the negative decimal number "-1.55". |
| log | Natural logarithm |
| sin | Sine |
| sinh | Hyperbolic sinus |
| sqrt | Root function (square root) |
| tan | Tangent |
| tanh | Hyperbolic tangent |

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Comparison operations:](javascript:void(0);)

| Format element | Meaning |
| --- | --- |
| < | Less than |
| <= | Less than or equal to |
| > | Greater than |
| >= | Greater than or equal to |
| = | Equal to |
| <> | Not equal to |
| regex | Comparison with regular expressions. Regular expressions conforming to the ECMA standard may be used in the subsequent constant. |
| wildcard | Comparison with placeholder characters. Placeholder characters may be used in the subsequent constant, just as they are used for searches in Eplan (see [Using Placeholders](searchandreplacegui_k_platzhalter.htm)). |

![](../Pictures/Gui/ALL/note.png)Notes:

- The format elements "=", ">", etc. compare an input value with a comparison value (constant). Depending on the type of the constant, the values are treated as numbers or as texts.
- The format elements "wildcard" and "regex" allow comparison operations of an input value with a complex comparison value. The input value and comparison value are always treated as text.
- The "wildcard" format element compares the complete character string in the comparison operation and allows the usage of the placeholder characters "\*" and "?" in the comparison value.
- The "regex" format element searches for a partial string in the input value. The comparison value is described as a regular expression.

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Comparison operations for series:](javascript:void(0);)

The comparison operators for series can be used to compare the entries in a semicolon-separated series of values with a specified comparison criterion.

| Format element | Meaning |
| --- | --- |
| ;< | Less than |
| ;<= | Less than or equal to |
| ;> | Greater than |
| ;>= | Greater than or equal to |
| ;= | Equal to |
| ;<> | Not equal to |
| ;regex | Comparison with regular expressions. Regular expressions conforming to the ECMA standard may be used in the subsequent constant. |
| ;wildcard | Comparison with placeholder characters. Placeholder characters may be used in the subsequent constant, just as they are used for searches in Eplan (see [Using Placeholders](searchandreplacegui_k_platzhalter.htm)). |

![](../Pictures/Gui/ALL/note.png)Notes:

- The format elements ";=", ";>", etc. compare an input value with a comparison value (constant). Depending on the type of the constant, the values are treated as numbers or as texts.
- The format elements "wildcard" and "regex" allow comparison operations of an input value with a complex comparison value. The input value and comparison value are always treated as text.
- The "wildcard" format element compares the complete character string in the comparison operation and allows the usage of the placeholder characters "\*" and "?" in the comparison value.
- The "regex" format element searches for a partial string in the input value. The comparison value is described as a regular expression.

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Boolean operations:](javascript:void(0);)

| Format element | Meaning |
| --- | --- |
| AND | AND operation |
| OR | OR operation |
| XOR | Exclusive OR (either or) |
| BOOL | Boolean value, binary value (true or false) |
| NOT | NOT operation |

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Statistical functions:](javascript:void(0);)

| Format element | Meaning |
| --- | --- |
| count | Quantity, counts values that are not equal to "0" (numerical value), are not equal to "0" or are "FALSE" (bool), are not equal to "0" or are empty (monolingual or multilingual string). |
| max | Maximum |
| median | Median value |
| min | Minimum |
| sum | Sum, totals the values that are transferred separated by semicolons. |

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)](javascript:void(0);)[Conditions:](javascript:void(0);)

IF conditions such as "if\_error", "if\_true", etc., allow calculation formulas with conditional calculations to be extended. This way you can, for example, output status messages or different calculation results depending on specific criteria. The conditions can also be nested in order to map more complex logics.

In contrast to classic programming languages, the IF conditions always have to return a value, also when the condition is not fulfilled. It is therefore obligatoty to use brackets to group the calculations.

When using the IF conditions in calculation properties, the sequence and the grouping by brackets must be observed correctly:

- The calculation formula that is to be evaluated (for example a comparison or a mathematical operation) must be specified first. This calculation formula must be grouped by brackets (format elements "(" and ")"). Several IF conditions are also possible in a calculation formula. These then also have to be grouped by brackets.
- This is followed by the IF condition, which checks the result of the calculation and returns values or texts accordingly.

| Format element | Meaning |
| --- | --- |
| if\_error | Allows a self-defined error text or a number to be displayed in the event of an error. This number can then be calculated further despite an error. The error text or the number are specified via a subsequent constant. |
| if\_true | Checks whether the preceding condition is true. If this is the case, the subsequent calculation is carried out or the subsequent value is output. |
| if\_false | Checks whether the preceding condition is false. If this is the case, the subsequent calculation is carried out or the subsequent value is output. |
| if\_else | Checks whether the preceding condition is not fulfilled. If this is the case, the subsequent calculation is carried out alternatively or the subsequent value is output. |

![](../Pictures/Gui/ALL/info.png)Tips:

Typical sources of errors and how to avoid them

- Incorrect sequence of the elements:  
  The IF condition must not precede the calculation formula. This results in the condition not being evaluated correctly. The calculation formula must always precede the IF condition.
- Missing or incorrect brackets:  
  Without the correct bracketing, the formula is not interpreted correctly. This leads to incorrect results or error messages. Each calculation to which an IF condition refers must be in parentheses.
- Multiple conditions without grouping:  
  If several IF conditions are used (for example several "if\_false"), these conditions must also be grouped by brackets in order to clearly structure the evaluation.
- Unclear or missing replacement texts:  
  If the values returned by the IF conditions are to be used as a status display, suitable [Replacement texts](eservicesgui_d_ersetzungstexte.htm) have to be defined so that comprehensible texts are output. A replacement text is an assignment of the return values of a calculation or condition to comprehensible, meaningful texts. For example, numeric values such as "0" or "1" that are returned by an IF condition can be replaced by easily readable status messages such as "warm" or "cold".
- Confusion of comparison operators:  
  Ensure that the comparison operators (for example ">", "<", "=", "!=") are used correctly and match the data types used.

Tips for troubleshooting

- Break down complex conditions into smaller parts and test them individually.
- Use simple test values to check the functionality of the IF condition.
- Check the brackets carefully, in particular in the case of nested conditions.
- Use replacement texts to clearly display the return value of the condition.

Other format elements:

| Format element | Meaning |
| --- | --- |
| Property | Opens the dialog Format: Property. Allows you to enter a property in the calculation formula. |
| Constant | Opens the dialog Format: Constant. Allows you to enter a constant number, a constant text or the circle constant Ï for the calculation formula. |

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example: Space requirement of a part](javascript:void(0);)

You want to determine the space requirement of a part from its width and height and take into account a space reserve of 5%:  
(Width \* Height) + ((Width \* Height) \* 5 : 100)

You have already entered the dimensions in the parts management in the Properties tab at the properties Width, Height and Depth (command path: Tab Master data > Command group Parts > Management).

You create a user-defined property with the assignment "Function", the field type "Value with unit" and the display type "Block property". Click [...] in the Block format field.

Select the Calculation format element in the dialog Format. Enter the following format elements in the specified sequence in the dialog Format: Calculation:

| Format element | Meaning |
| --- | --- |
| ( | Select the "(" format element under the Basic arithmetic operations node. |
| Property | Double-click the Property format element. In the subsequent dialog Format: Property click the  character in front of the Part format element and select the subordinate format element Parts general and the Width property in the property list. |
| \* | Select the "\*" format element under the Basic arithmetic operations node. |
| Property | Double-click the Property format element. In the subsequent dialog Format: Property click the  character in front of the Part format element and select the subordinate format element Parts general and the Height property in the property list. |
| ) | Select the ")" format element under the Basic arithmetic operations node. |
| + | Select the "+" format element under the Basic arithmetic operations node. |
| ( | Select the "(" format element under the Basic arithmetic operations node. |
| ( | Select the "(" format element under the Basic arithmetic operations node. |
| Property | Double-click the Property format element. In the subsequent dialog Format: Property click the  character in front of the Part format element and select the subordinate format element Parts general and the Width property in the property list. |
| \* | Select the "\*" format element under the Basic arithmetic operations node. |
| Property | Double-click the Property format element. In the subsequent dialog Format: Property click the  character in front of the Part format element and select the subordinate format element Parts general and the Height property in the property list. |
| ) | Select the ")" format element under the Basic arithmetic operations node. |
| \* | Select the "\*" format element under the Basic arithmetic operations node. |
| Constant | Select the Constant format element. In the subsequent dialog Format: Constant select the Number option and enter the value "5". |
| / | Select the "/" format element under the Basic arithmetic operations node. |
| Constant | Select the Constant format element. In the subsequent dialog Format: Constant select the Number option and enter the value "100". |
| ) | Select the ")" format element under the Basic arithmetic operations node. |
| Separator | Close the Format: Calculation dialog. In the Format dialog, select the Separator format element. Enter a blank followed by the unit in the subsequent dialog, in this example "mm²". |

After you have configured the property, it is automatically available at all functions in the project.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example: Total power (pre-planning)](javascript:void(0);)

You want to determine the sum of the power that is entered at the PCT loops (consumers) in the pre-planning.

You have entered the outputs at the PCT loops (consumers) in the supplied user-defined property Eplan.PCT loop.Power.

You create a user-defined property with the assignment "Pre-planning", the field type "Value with unit" and the display type "Block property". Click [...] in the Block format field.

Select the Calculation format element in the dialog Format. Enter the following format elements in the specified sequence in the dialog Format: Calculation:

| Format element | Meaning |
| --- | --- |
| sum | Select the "sum" format element under the Statistical functions node. |
| Property | Double-click the Property format element. In the subsequent dialog Format: Property click the  character before the format element All subordinate segments and select the subordinate format element PCT loops and in the property list the Eplan.PCT loop.Power property. |
| Separator | Close the Format: Calculation dialog. In the Format dialog, select the Separator format element. Enter a blank followed by the unit in the subsequent dialog, in this example "kW". |

After you have configured the property, you must make it visible for the desired segments (in this case the PCT loops). To this purpose you have to assign the property in the dialog Configure segment definitions to the corresponding segment definitions (in the tab Segment properties). The command path to the dialog is: Tab Pre-planning > Command group Edit > Configure segment definitions.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example: Number of temperature measurements at PCT loops (pre-planning)](javascript:void(0);)

The project contains several PCT loops, some of which are used for temperature measurements. Now you want to determine the number of temperature measurements in the pre-planning. This includes all PCT loops for which the identifier "T" is entered in the Measurand property of the PCT loop number.

You create a user-defined property with the assignment "Pre-planning", the field type "Value with unit" and the display type "Block property". Click [...] in the Block format field.

Select the Calculation format element in the dialog Format. Enter the following format elements in the specified sequence in the dialog Format: Calculation:

| Format element | Meaning |
| --- | --- |
| count | Select the "count" format element under the Statistical functions node. |
| ( | Select the "(" format element under the Basic arithmetic operations node. |
| Property | Double-click the Property format element. In the subsequent dialog Format: Property click the  character before the All subordinate segments format element and select the subordinate format element PCT loops and in the property list the Measurand property. |
| ;= | Select the format element ";=" under the node Comparison operations for series. |
| Constant | Select the Constant format element. In the subsequent dialog Format: Constant select the Text option and enter the value "T". |
| ) | Select the ")" format element under the Basic arithmetic operations node. |
| Separator | Close the Format: Calculation dialog. You can use a separator to define an additional (non-translatable) text for the display. In the Format dialog, select the Separator format element. Enter a blank followed by a text in the subsequent dialog, for example "Number of temperature measurements". |

After you have configured the property, you must make it visible for the desired segments (in this case the PCT loops). To this purpose you have to assign the property in the dialog Configure segment definitions to the corresponding segment definitions (in the tab Segment properties). The command path to the dialog is: Tab Pre-planning > Command group Edit > Configure segment definitions.

![](../Pictures/Gui/ALL/info.png)Tip:

If you want to display a translatable text together with the calculation property, you cannot define it using a separator in the format. As an alternative you can insert a "normal" multilingual text later during the configuration next to the component for which the calculation property is used and group it with the property text of the component.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example: Use of conditions](javascript:void(0);)

You want to display at a function whether the operating temperature is higher or lower than 25°C. If this is the case, the text "warm" should be displayed, otherwise "cold". To this purpose you use a block property, here Block property [1].

In the associated format property Block property: Format [1] you determine the operating temperature and output the desired display texts via the conditions "if\_true" and "if\_else".

Step 1: Specify the conditional status display

Select the Block property: Format [1] property in the property dialog of the function and click [...] in the Value column. Select the Calculation format element in the dialog Format. Enter the following format elements in the specified sequence in the dialog Format: Calculation:

| Format element | Meaning |
| --- | --- |
| ( | Select the "(" format element under the Basic arithmetic operations node. |
| Property | Double-click the Property format element. In the subsequent Format: Property dialog, click the  character in front of the Part reference format element and select the Part reference 1 subordinate format element and the Operating temperature property in the property list. |
| > | Select the ">" format element under the Comparison operations node. |
| Constant | Select the Constant format element. In the subsequent dialog Format: Constant select the Number option and enter the value "25". |
| ) | Select the ")" format element under the Basic arithmetic operations node. |
| if\_true | Select the "if\_true" format element under the Conditions node. |
| Constant | Select the Constant format element. In the subsequent Format: Constant dialog, select the Text option and enter the value "warm". |
| if\_else | Select the "if\_else" format element under the Conditions node. |
| Constant | Select the Constant format element. In the subsequent Format: Constant dialog, select the Text option and enter the value "cold". |

Step 2: Display the block property at the function

After you have configured the block property, you want to make it visible at the function in the graphical editor. To this purpose, select the Block property [1] properties as the display property in the Display tab of the property dialog of the function.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example: Usage of conditions together with replacement texts](javascript:void(0);)

After you have determined the space requirement of a part, you want to display at the device in the schematic whether the device is suitable or is too large. To this purpose you use two block properties and the associated format properties at the function (in this case: Block property [11], Block property: Format [11], Block property [12], Block property: Format [12]).

Step 1: Determine the space requirement

In the Block property: Format [11] property you determine the space requirement of the part. Select this property in the property dialog of the function and click [...] in the Value column. Select the Calculation format element in the dialog Format. Enter the formula for calculating the space requirement in the Format: Calculation dialog as described above in the example "Space requirement of a part".

Step 2: Specify the conditional status display

In the Block property: Format [12] property specify by means of an "if\_true" condition whether the calculated space requirement is suitable for the usage of the device or is too large. The condition checks whether the value calculated in the Block property: Format [11] is greater than "105". If the condition is true, the value "1" is returned, otherwise "0".  
To display whether the condition is met or not, use a local table with replacement texts. (Such a table is saved directly in block format.) The corresponding status is then output via the output texts specified there.

Select the Block property: Format [12] property in the property dialog of the function and click [...] in the Value column. Select the Calculation format element in the dialog Format. Enter the following format elements in the specified sequence in the dialog Format: Calculation:

| Format element | Meaning |
| --- | --- |
| Property | Double-click the Property format element. In the subsequent Format: Property dialog click the  character in front of the Function format element and select the General function subordinate format element and the Block property [11] property in the property list. |
| if\_true | Select the "if\_true" format element under the Conditions node. |
| Property | Double-click the Property format element. In the subsequent Format: Property dialog click the  character in front of the Function format element and select the General function subordinate format element and the Block property [11] property in the property list. |
| > | Select the ">" format element under the Comparison operations node. |
| Constant | Select the Constant format element. In the subsequent dialog Format: Constant select the Number option and enter the value "105". |
| Separator | Close the Format: Calculation dialog. In the Format dialog, select the Separator format element. Enter a blank in the subsequent dialog. |
| Property | Double-click the Property format element. In the subsequent Format: Property dialog click the  character in front of the Function format element and select the General function subordinate format element and the Block property [12] property in the property list. |
| Replacement text | In the Replacement text field, select "Use local table" and click [...]. In the Replacement texts dialog add two lines by means of the  button:   | Operator | Source text | Output text | | --- | --- | --- | | = | 0 | The device is OK. | | = | 1 | The device is too large. | |

Step 3: Display the block properties at the function

After you have configured the block properties, you want to make it visible at the function in the graphical editor. To this purpose, select the Block property [11] and Block property [12] properties as display properties in the Display tab of the property dialog of the function.

Selected format elements:

This field contains the format elements comprising the format of the calculation formula.

To replace an operator in a calculation formula that was marked in the Selected format elements field by another operator, mark the desired new operator in the Available format elements field and click ![](../Pictures/Gui/ALL/all_assign_as.png) (Replace).

The selected format elements are displayed in table form with their designation, with their symbol (for example the property number at selected properties), with their schematic representation of the set format (for the preview) and - if available - with their value.

The order of the format elements in the list determines the order in which the individual format elements are used in the calculation formula. You can change the sequence using the arrow buttons on the toolbar.

The following can be displayed in the Preview column:  
(This column is not available for calculations in the form and plot frame editor.)

- Values of properties: If you have marked an object before the definition of the block format and have selected a property as the format element, the property values of this object are displayed. If the property has an alphanumeric value (for example "6A"), the numerical value is additionally displayed in brackets (for example "6A (6)").
- Values in calculations: In the row of calculation functions, operators, closing brackets, etc., the concrete values of calculations and - in brackets - the associated operation are displayed. Here you can, for example, recognize whether the numerical or alphanumeric value of a property is used (for example alphanumeric value: "1 (6A = "6A")", numerical value: "1 (6 = 6)"). Constants of the type "Text" are displayed in quotation marks "".
- Error messages: If calculations cannot be carried out due to errors during the selection of format elements, error messages are displayed (for example "Calculation error: Division by 0. (1/0)").

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Examples:](javascript:void(0);)

- You have selected a function in the schematic and select the "DT (displayed)" format element. The displayed DT of the selected function is then displayed in the Preview column.
- You have marked a function in the schematic and select the "Function / technical characteristics" format element. The value "6A" is entered for the technical characteristics at the function. The alphanumeric value is then displayed in brackets in the Preview column, followed by the numerical value: "6A (6)".
- You have defined a calculation formula in which the technical characteristics of a function are compared with a specific value, for example with the value "6A". In the row with the operator "=" the following value is displayed in the Preview column: "1 (6A = "6A")". The number "1" indicates that the statement of the comparison operation is true.

Navigation:

If the calculation formula contains properties and you have selected one of these properties in the Selected format elements field, a navigation path is displayed here. This way you can recognize more easily from which object the property is determined.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

The "Part / width" format element is marked in the Selected format elements field, and the values "Main function" and "Part" are displayed below each other in the Navigation field. This means that, starting from a function, the main function is searched for first. Subsequently, the part stored at the main function is determined and the value entered in the part property Width is displayed.

Minimum number of digits:

The value in this field defines the minimum number of digits to be used for the result. This value only affects the positions before the decimal point. If the result has fewer digits before the decimal point than specified here, then the missing digits are padded with the filler character you have defined in the Filler character field.

Filler character:

Missing positions are filled in with this filler character. The number of digits filled depends on the value you entered in the Minimum number of digits field.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

If you entered a value of "4" in the Minimum number of digits field and the value "0" as the Filler character, numbers are output as follows: "0001".

Decimal places:

The value in this field defines the minimum number of decimal places displayed in the result. If the result has more digits than allowed after the decimal point, then the digits are rounded to the permitted number of decimal places. If the result has fewer digits than allowed after the decimal point, then the missing digits are padded with the filler character you have defined in the Filler character field.

Decimal places, variable:

If this check box is selected then decimal places are never padded with filler characters. If the result has fewer digits than allowed after the decimal point, then the result remains unchanged. If the result has more digits than allowed after the decimal point, then the digits are rounded to the permitted number of decimal places.

Hide output if:

In this group box you define the conditions under which the project / parts data is not output. Select an operator from the drop-down list. Enter a value for the condition in the field below this.

- Do not hide: No condition applies for the project data. The selected project data is always output.
- =: The selected project data is not output when it has a value equal to that specified in the field below.
- >: The selected project data is not output when it has a value greater than that specified in the field below.
- <: The selected project data is not output when it has a value smaller than that specified in the field below.
- >=: The selected project data is not output when it has a value greater than or equal to that specified in the field below.
- <=: The selected project data is not output when it has a value smaller than or equal to that specified in the field below.
- <>: The selected project data is not output when it has a value unequal to that specified in the field below. Only project data having this value is output.

Including preceding separator:

If this check box is selected then the project data satisfying the conditions in the fields above is not output, including any preceding separator. This setting affects the Separator format element preceding the Calculation format element.

Including following separator:

If this check box is selected then the project data satisfying the conditions in the fields above is not output, including any subsequent separators. This setting affects the Separator format element coming after the Calculation format element.

See also

[Dialog Format: Block property](eservicesgui_d_formatblockeigenschaft.htm)

[Dialog Format: Separator](eservicesgui_d_formattrennzeichen.htm)

[Dialog Format: Comment](eservicesgui_d_formatkommentar.htm)

[Defining Block Properties](blockproperties_h_blockeigenschaftendefinieren.htm)