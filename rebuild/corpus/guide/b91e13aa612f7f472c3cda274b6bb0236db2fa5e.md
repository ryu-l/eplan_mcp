# Routing Connections

Routing generates routing connections between the 3D part placements that follow the real course of the wires and cables in the routing path network. If there are no connection points defined for a part placement, they will be generated prior to the routing.

Apart from the routing track, the length of wires is also determined. All information thus determined can be used as wiring instructions or to control wire fabrication machines.

Precondition:

You have opened a project. A layout space is open. The routing path network is generated.

1. Select the following commands: Tab Edit > Command group Routing connections > Route.  
     
   ![](../Pictures/Gui/ALL/arrow.png) In the status bar, you are prompted to select the connections to be routed.
2. Enter the two corner points of a rectangle that comprises the part placements and connections to be routed.  
     
   ![](../Pictures/Gui/ALL/arrow.png) The most efficient routing track is calculated. The routing connections are displayed in 3D representation.  
     
   ![](../Pictures/Gui/ALL/arrow.png) The routing connections are assigned the properties Length, Routing track, Routing track specification, Connection dimension, Routing direction, Wire termination processing, and Dual sleeve prescribed.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

Routing connection through three wire ducts

![](../Pictures/Visualisation/ALL/routing_routedconnection_as.png)

![](../Pictures/Gui/ALL/info.png)Tip:

Routing connections is also possible in the [Wiring manager](routinggui_d_verdrahtungsmanager.htm) dialog. This dialog offers the advantage that only the connections relevant for the routing and wiring are displayed. Via the selection in the layout space navigator and an additional filter you can, for example, display the connections of a specific enclosure that have not yet been routed. To route the desired connections, select these in the dialog and select the Route popup menu item.

See also

[Generating a Routing Path Network](routinggui_h_streckennetzerzeugen.htm)

[Changing a Route](routinggui_h_verlegewegaendern.htm)

[Management of Routing Connections](routinggui_k_verdrahtungsmanager.htm)