# Eplan Fluid: Complex Devices

### Black Boxes and Device Connection Points

In fluid power, complex devices such as valve blocks or power units with internal connection points are often used. In fluid power schematics, such devices are normally displayed graphically within rectangles. Eplan Fluid supports this display method and provides relevant functions for implementing black boxes and device connection points.

If you insert a black box or device connection point in the schematic, the program checks which page type this happens on. If it is "fluid power schematic"-type page, the corresponding function definition is automatically configured, and you insert a "black box (fluid)" or "device connection point (fluid)".

### Device groups

Complex fluid power devices with electrical engineering components can be implemented with the help of a device group. Functions from different trades and devices tags can be grouped graphically and logically into one device.

The functions combined as a device group are as a rule saved as a macro. The creation of a device group is therefore only possible in a macro project. To group devices you must first select the functions to be grouped in the schematic. Then select the following commands: Tab Edit > Command group Group > Device. If the criteria for the creation of a device group have been met, the functions will be grouped.

See also

[Black Boxes](blackbox_k_start.htm)

[Inserting Device Connection Points](blackbox_h_anschluesseeinfuegen.htm)

[Device Groups](devicelistgui_k_geraetegruppierung.htm)