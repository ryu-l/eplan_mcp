# Generating Parts Based on Schematic Data

In Eplan, a part can be generated from the data stored at a component. Among other data, the part and device-specific data (function definition, connection point designation, etc.) is transferred to the parts database. A part generated in this way contains function templates for all device functions and is stored as a part in the project. If subdevices are assigned to the device, the device-specific data of the subdevices is transferred into the part.

Preconditions:

- You have opened a project. You have opened a project page in the graphical editor.
- You have marked a component that represents a main function.
- Exactly one parts reference is entered in the DT.

1. Select the Generate part popup menu item.  
     
   ![](../Pictures/Gui/ALL/arrow.png) The Parts management dialog opens. The new part is sorted into the product group structure of the parts management module with reference to the function definition for the component, and is preset.  
     
   ![](../Pictures/Gui/ALL/arrow.png) If a part already exists in the project, a query is displayed that you have to confirm to overwrite. If the part does not exist yet in the parts database, the parts management is subsequently opened, and the new part is pre-selected.  
     
   ![](../Pictures/Gui/ALL/arrow.png) If a part already exists in the parts database, a further security prompt is displayed. If you confirm this prompt with [Yes], the device-specific data (such as the function templates) are updated at the parts already existing in the parts database. In this case the parts management is opened and the part is pre-selected. If you click [No], the parts existing in the project are overwritten. However, the part in the parts database is not updated.
2. If necessary, you can edit the data of the new part on the tabs on the right.
3. Click [Close].  
     
   ![](../Pictures/Gui/ALL/arrow.png) Exits the Parts management dialog.   
     
   ![](../Pictures/Gui/ALL/arrow.png) If data that may have been changed is not saved via [Apply] then an additional confirmation prompt is displayed. A message is also displayed allowing you to decide whether an automatic "Parts database -> Project" parts data synchronization should be performed.

![](../Pictures/Gui/ALL/note.png)Note:

Parts generated in this way have the value "NEW" assigned for the Function group property on the Properties tab. This allows you to specifically filter for such parts.

### Generating prefabricated cables as a module

If you have configured a prefabricated cable in the schematic (cable with cable definition as main function, one or two plug definitions with plugs as subordinate devices), you can also generate a new module for prefabricated cables from a new part number at the cable by using the command Generate part in the parts management.

A new part has to be entered at the cable in the Parts tab so that a module can be generated, and the "Module" record type has to be set for the part reference property Record type. You can use a new or an existing part for the subordinate plug definitions. Select the "Component" entry for the record type in the Parts tab here, and activate the check box of the part reference property Part is included in a module.

During this process, the data of the cable and plugs is transferred to the module. A module generated in this way contains the function templates for all functions of the pre-defined cable and is stored as a part in the project. The subordinate plug or plugs of the cable are entered as subparts for the module in the Module tab. If a part entered at a plug definition does not yet exist in the parts management, this part is also generated (as a component).

See also

[Parts Behavior when Selecting Devices](partselectiongui_k_prinzip.htm)

[Prefabricated Cables](cablegui_k_vorkonfektioniertekabel.htm)

[Synchronizing Stored Parts with the Parts Database](partselectiongui_h_artikeldateneinlagern.htm)

[Deleting Stored Parts Properties](partselectiongui_h_eingelagerteartikeleigenschaftenloeschen.htm)

[Dialog Parts management](articlesgui_d_artverw.htm)