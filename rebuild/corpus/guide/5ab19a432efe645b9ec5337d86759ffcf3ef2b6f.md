# Dialog External editing: Import parts data

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Dialog call:](javascript:void(0);)

Tab Master data > Command group Parts > Management. In the Parts management dialog you have opened a parts database using [Extras] > Settings. Button [Extras] > Edit properties externally > Import data.

In this dialog you specify the import settings for the data of the parts management which where edited externally after an export. The dialog is not opened when you have selected the Edit in external application and reload option during the export.

Overview of the main dialog elements:

File name:

Enter the name of the import file here, or click ![](../Pictures/Gui/ALL/all_selectfile_as.png) to select it interactively.

Record identification:

- Identification via object ID: With this option the data is identified via the Object ID of the parts management - as is usual during importing.
- Identification via name: With this option the data is identified via its name. Parts, for example, are identified via their part number and their variant designation and data records for addresses (customer, manufacturer / supplier), for example, via their short names.   
  With this identification you can import parts data which were created in external systems into the own parts database.

Importing:

With the options in this group box you specify the following settings for the import behavior:

- Add new records only: Existing records are not changed. Only new ones are added.
- Replace existing records only: Existing records are replaced completely by the records from the import file. New records are ignored.
- Replace existing records and add new ones: Existing records are replaced completely by the records from the import file. New records are added.

See also

[External Editing: Functionality](pxfinterface_k_arbeitsweise.htm)

[Exporting Data for External Editing](pxfinterface_h_seitenexportieren.htm)

[Importing Externally Edited Properties](pxfinterface_h_datenimportieren.htm)