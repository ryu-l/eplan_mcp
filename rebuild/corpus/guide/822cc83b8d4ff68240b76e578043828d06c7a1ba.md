# Online Numbering of Fluid DTs (ISO 1219-2)

When you use norm-conform identification to ISO 1219-2 for fluid devices, Eplan offers the following online numbering possibilities:

- Numbering of the Item numbers On symbol insertion
- Optionally numbering of the Circuit or the Item number On insertion of macros and copies.

The following section provides examples of how you can use these numbering options.

Preconditions

- You have opened a project.
- The project was created by using the FL\_1219-2\_bas001.zw9 basic project. This basic project has the effect that fluid device tags are structured to ISO 1219-2 with a trade-specific Media code, a Circuit number and an Item number.
- You have created a page of the type "Fluid power schematic" and have opened the page in the graphical editor.

### Numbering item numbers on insertion of symbols

1. Insert a symbol from one of the installed fluid symbol libraries into the schematic page.  
     
   ![](../Pictures/Gui/ALL/arrow.png) In the Displayed DT field a device tag is suggested automatically that disposes of a trade-dependent media code (depending on the trade of the device last inserted), a circuit number (beginning with "0"), a separator (.) as well as an item number. If there is no device in the schematic, the item number in the DT begins with "1".
2. Click [OK] to close the property dialog and place the symbols further times. Observe the numbering of the devices in the Displayed DT field of the property dialog respectively.  
     
   ![](../Pictures/Gui/ALL/arrow.png) The circuit number remains unchanged at all devices, while the item number is increased by "1" after each insertion.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

You insert a pneumatic valve three times consecutively on a fluid schematic page on which five pneumatic valves with the displayed DT P0.1 to P0.5 are already placed. The following displayed DT is then assigned to the three added pneumatic valves:  
P0.6, P0.7 and P0.8

#### Numbering an item number or circuit number on insertion of macros and copies

When inserting macros and when copying symbols you can specify whether the Item number or the Circuit number is to be incremented in the DT during online numbering.

1. Delete all the symbols on the schematic page.
2. Insert a window macro from one of the installed fluid macro directories into the schematic page. Or copy a fluid-specific symbol that is already inserted into the Clipboard and insert it into the schematic page.
3. Move the macro or symbol attached to the cursor to the desired position in the schematic and place it with a mouse-click in the schematic.
4. First deactivate the Number circuit number if existing check box in the Insertion mode dialog.
5. Click [OK].
6. Place the macro or the symbol several times into the schematic.
7. Look at the numbering of the devices just inserted.  
     
   ![](../Pictures/Gui/ALL/arrow.png) The deactivated Number circuit number if existing setting has the effect that the item number of the fluid device that is identified in accordance with ISO 1219-2 is increased by "1" whenever the macro or the symbol is inserted. The circuit number by contrast remains unchanged.
8. Insert the macro or the copied symbol several times into the schematic again.
9. This time activate the Number circuit number if existing check box in the Insertion mode dialog.
10. Look again at the numbering of the inserted device copies.  
      
    ![](../Pictures/Gui/ALL/arrow.png) The activated Number circuit number if existing setting has the effect that the circuit number of the fluid device that is identified in accordance with ISO 1219-2 is increased by "1" whenever the macro or the symbol is inserted. The item number by contrast remains unchanged.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

The schematic page contains three pneumatic valves whose displayed DTs are P0.1, P0.2 and P1.6. You copy the pneumatic valve with the DT P0.1 into the Clipboard and insert it three times on the fluid schematic page.

First deactivate the check box. After insertion the three new pneumatic valves have the following displayed DTs: P0.3, P0.4 and P0.5

Abort the process and insert the 3 pneumatic valves again. This time activate the check box. After insertion the three new pneumatic valves have the following displayed DTs: P1.1, P2.1 and P3.1.

See also

[Eplan Fluid: DT Numbering](ftechnic_k_bmknummerierung.htm)

[Online Numbering](numerationgui_k_start.htm)

[Eplan Fluid: Device Structure](ftechnic_k_betriebsmittelstruktur.htm)

[Dialog Settings: DT syntax check](devicetagcheckgui_d_einstellungenprojektbmsyntaxueberpruefung.htm)

[Dialog Settings: Numbering (online)](numerationgui_d_einstellnummerierung.htm)