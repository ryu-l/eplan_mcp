# P017010: Overlapping structure boxes contain contradictory structure identifiers

### Cause

In a project in which the product aspect is used for structuring, there are two overlapping structure boxes containing different structure identifiers with regard to one or several identifier blocks.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

![](../Pictures/Visualisation/ALL/messages_017010_bsp1_av.png)

The structure box with identifier +O1 overlaps with the structure box which is characterized by =A1+E1. With regard to the device -F1, the structure identifiers are contradictory for the identifier block "Location designation" (O1 and E1).

### Solution

#### Possibility 1

1. Locate the first overlapping structure box in the schematic using the Go to (graphic) function from the popup menu in the Message management dialog.
2. Call up the Properties <...> dialog of the structure box and bring the Identifier tab to the foreground.
3. Adjust the structure identifier for the respective identifier block to the identifier structure of the second structure box and confirm with [OK].

#### Possibility 2

1. Locate the second overlapping structure box in the schematic by using the functionality Go to (2nd coordinate), which the popup menu of the Message management dialog offers you.
2. Call up the Properties <...> dialog of the structure box and bring the Identifier tab to the foreground.
3. Adjust the structure identifier for the respective identifier block to the identifier structure of the first structure box and confirm with [OK].

---

  

If required, start a new check run.