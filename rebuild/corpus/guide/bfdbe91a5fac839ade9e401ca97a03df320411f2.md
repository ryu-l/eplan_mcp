# Selecting Cables

Cables can be selected automatically or manually.

Using the Allow manual cable selection only property for each cable you can specify that the cable can only be manually selected. Those types of cables are then not considered for the automatic cable selection.

### Select the cables manually

Select a part from the parts database when making manual cable selections. The procedure is exactly like selecting any other part. You have the following options:

- [Selecting parts](partselectiongui_h_artikelauswahl.htm) in the Properties <...> dialog
- [Selecting parts in the navigators or the device list](partselectiongui_h_artikelauswahlnavigator.htm)

### Select cables automatically

The automatic cable selection only exchanges the main part (this is the first part from the "Cable" product group) with a suitable part from the parts management.

In the project settings you can define a preselection list of cable types (cable parts) for the automatic cable selection and save them as a scheme. Only the cables from this list are used for automatic cable selection.

Preconditions:

- You have selected cable definitions in the graphical editor, or in the Cables - <Project name> navigator dialog.
- You have created a scheme with settings for the automatic cable selection ([Dialog Settings: Automatic cable selection](cablegui_d_einstellungenautomatischekabelauswahl.htm)). In this scheme you have specified which properties are to be considered for the automatic cable selection.

1. Select the following commands: Tab Connections > Command group Cables > Drop-down button Reassign all > Automatic cable selection.
2. In the [Automatic cable selection](cablegui_d_automatischekabelauswahl.htm) dialog, determine the settings for automatic cable selection.
3. Click [OK].  
     
   ![](../Pictures/Gui/ALL/arrow.png) According to the settings in the selected scheme the fitting cable is searched in the parts management. The properties specified in the scheme as well as the sequence of the entries are considered. For example, the voltage entered at the cable, the voltage of the cable connections (defined via the potentials), the number of connections existing, and the number of spare cable connections are compared. If there is no information on the manufacturer, the first one to be found will be taken.

![](../Pictures/Gui/ALL/note.png)Notes:

- The quantity of PE / PEN conductors (0, 1, or 2) specified in the PE conductors field is taken into consideration when determining the quantity of connections. If the cable has no PE / PEN conductors and there is no cable in the corresponding voltage range without PE / PEN conductors in parts management, then a cable with PE / PEN conductors will be taken. The PE / PEN conductor is added to the number of connections needed.
- Arbitrary values can be predefined for the individual cables. This way, for example, a cable type can already be entered on a cable in the schematic that is not in the settings for the cable selection. A cable will then be selected which corresponds to the cable type entered and has a suitable number of connections. A missing cross-section / diameter is taken from the defaults depending on the voltage. The same values are used here as for manual cable selection.

See also

[Cable Behavior](cablegui_k_prinzip.htm)