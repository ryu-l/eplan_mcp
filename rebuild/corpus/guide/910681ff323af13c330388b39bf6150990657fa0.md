# P005039: Representation types of targets differ

### Cause

The target functions existing at a connection have a conflicting entry for the Representation type property. For example, the "Multi-line" representation type was set at the first target and "Single-line" was set at the second target.

### Solution

1. Assign the same representation type to the targets of the connection.
2. If required, start a new check run.