# Working with Grids

You can work with a grid to make the positioning of elements easier. The insertion and element points are then placed on the grid points. A line grid is used for working in a 3D layout space. In this case, the insertion points are on the points where the lines cross.

The size of the grid used (i.e. the clearance between the individual grid points) is stored as a page property and displayed in the status bar. You can select one of the default grid sizes from the drop-down list or enter you own values. With layout spaces, the size of the grid is not a page property.

You can store up to five different grid sizes in the user settings. These can then be selected by using the drop-down button ![](../Pictures/Gui/ALL/all_gridactive_as.png) (Grid) in the status bar. This setting is possible separately for 2D and 3D.

Precondition:

You have opened a project. You have opened an editor such as the graphical editor, the plot frame editor, etc.

### Display the grid

1. Click the button ![](../Pictures/Gui/ALL/all_gridactive_as.png) (Grid) in the status bar to switch the grid display on or off.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

Schematic section: Grid view off

![](../Pictures/Visualisation/ALL/gededitgui_gridoff_av.png)

Schematic section: Grid view on

![](../Pictures/Visualisation/ALL/gededitgui_gridon_av.png)

### Use snap to grid

The use of grid snap is independent of the grid display options.

1. Click the button ![](../Pictures/Gui/ALL/statusbar_snaptogrid_as.png) (Snap to grid on / off) in the status bar to activate the Snap to grid.  
     
   ![](../Pictures/Gui/ALL/arrow.png) If the grid snap is activated, then all of the subsequent actions are performed within the grid points.

### Align on grid

This function allows you to subsequently align objects to the grid settings without having initially drawn them using the grid. If you subsequently switch on the grid snap, then previously drawn objects may no longer be accessible if they aren't on a grid point.

1. Select the objects and then select the following commands: Tab Edit > Command group Arrange > Align on grid.  
     
   ![](../Pictures/Gui/ALL/arrow.png) The selected objects are realigned (i.e. moved) so that all of their important points (such as line starting points, curve ends etc.) are on the grid.

See also

[Graphical Editor](gededitgui_k_start.htm)

[Dialog Settings: 2D](gedviewer_d_einstellungenbenutzerallgemein.htm)