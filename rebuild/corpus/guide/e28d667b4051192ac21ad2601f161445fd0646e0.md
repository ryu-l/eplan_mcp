# Dialog Settings: Tree structure (...)

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Dialog call:](javascript:void(0);)

You have opened a project. File > Settings > Projects > "Project name" > Display > Tree structure (navigators) or Tree structure (pages).

These dialogs allow you to define the display and order of the information for the tree structure device- and page-specifically so that only the information relevant to you is displayed in the tree view of the navigators.

![](../Pictures/Gui/ALL/note.png)Notes:

- These settings do not influence the list displays of the navigators. These still display the logical sequence of the pages and project data.
- Please note that these settings are ignored when importing and exporting project settings!

Overview of the main dialog elements:

Tree structure:

Here the structure identifiers and properties are listed which are displayed as nodes in the tree structure. You can use the buttons above the list to show or hide further structure identifiers or properties as nodes as well as modify the sequence of the displayed nodes.

Also show descriptive identifiers:

Select this check box when the identifying and describing structure identifiers should be displayed in the navigator tree views. If this check box is deselected then only the identifying project structures are displayed.

Detached representation of substructures:

If this check box is selected, the substructures of structure identifiers are displayed as separate nodes in the tree views of the navigators.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

You have activated the check box and specified a structure identifier for the identifier block "Function designation", designating it New.2. The level New and the sublevel 2 below this are displayed in the tree structure. If the check box is deselected, then only one level New.2 is displayed.

Also use representation sequence for page outputs:

This check box is only available in the dialog Settings: Tree structure (pages).

If the check box is activated, the sequence for the tree structure of the page navigator specified in this dialog is also used for the following functionalities:

- Scroll through pages in the graphical editor
- Printing pages
- Exporting pages as a PDF file
- Exporting pages in DXF / DWG format
- Output pages in the properties Next page / Previous page.

If the check box is deactivated, the sequence f the pages in the list view is decisive for the page output.

![](../Pictures/Gui/ALL/note.png)Note:

The logical sequence of the pages in the list view of the page navigator is decisive by default at the page output (and at other functionalities).

See also

[Specifying the Representation of the Tree Structure in the Navigators](projectstructure_h_kennzeichenausblenden.htm)