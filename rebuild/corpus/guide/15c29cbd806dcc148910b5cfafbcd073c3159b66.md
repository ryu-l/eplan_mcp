# Creating Device Cross-references

A device may be composed of different items for which different components may be used for the graphical display in the schematic. The components belonging to a device may be distributed over several project pages; in this case we refer to a "distributed display" of the device. All components belonging to the same device receive the same device tag. Eplan automatically adds a cross-reference to all components with the same device tag.

Preconditions:

- You have opened a project that contains several multi-line schematic pages.
- You have defined the display settings for cross-references under File > Settings > Projects > "Project name" > Cross-references / contact image > General.
- You have placed a function on one of the schematic pages (e.g. a contactor coil).
- You have placed further functions on other schematic pages (e.g. NO contacts).
- All functions have different device tags.

1. Select the following commands: Tab Devices > Command group Devices > Navigator.
2. Select all newly inserted functions in the device navigator.
3. Select the Properties popup menu item.
4. Specify the DT that is to receive all selected functions in the Displayed DT field of the <Function category> tab in the Properties <...> dialog.
5. Click [OK]..  
     
   ![](../Pictures/Gui/ALL/arrow.png) Cross-references are now generated and displayed at the respective components in the schematic. The display of the cross-references depends on the display settings.

### Create device cross-references between multi-line and single-line representations

Cross-references can also be created between devices in schematic pages having multi-line and single-line representations.

Preconditions:

- You have opened a project.
- You have loaded a multi-line and a single-line symbol library into your project.
- You have created a multi-line schematic page.
- You have created a single-line schematic page.
- You have specified in the Display device cross-references group box that cross-references between multi-line and single-line representations and vice-versa are to be displayed, under File > Settings > Projects > "Project name" > Cross-references / contact image > Display.

1. Open the multi-line schematic page and insert a multi-line symbol from the Multi-line symbol library.
2. Enter the device tag into the properties dialog and click [OK].
3. Open the single-line schematic page and insert an appropriate symbol from the Single-line symbol library.
4. Enter the same device tag into the property dialog and click [OK].
5. Open both pages in separate editors to observe the cross-reference display.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

On a multi-line schematic page you have inserted a 3-pole switch from a multi-line symbol library and given it the device tag "-Q1" (left side of the illustration). You have also inserted the same symbol (in single-line representation) into a single-line schematic page from a single-line symbol library and given it a device tag of "-Q1" (right side of the illustration). The cross-reference is now generated and displayed with the components on both pages.

![](../Pictures/Visualisation/ALL/xessettingsgui_allpol_einpolQVWerzeugen_av.png)

The 3-pole switch "Q1" on the multi-line schematic page (left) points to the 3-pole switch "Q1" on the single-line representation (right) and vice-versa.

See also

[Cross-references: Basics](xessettingsgui_k_grundlagen.htm)

[Cross-references: Cross-reference Types](xessettingsgui_k_querverweisarten.htm)

[Dialog Settings: General (projects, cross-references / contact image)](xessettingsgui_d_einstellungenprojektqvwallgemein.htm)