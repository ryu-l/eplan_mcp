# Exporting and Importing the Layer Management Configuration

The export and import of layer configuration files (\*.elc) allows you to save the layer configuration of the current project as a standard setting, to transfer this to another project or to make it available to another user.

Preconditions:

- You have opened a project.
- You have opened the layer management: Tab Tools > Command group Management > Layers.

### Export the layer management configuration

1. In the Layer management - <Project name> dialog click ![](../Pictures/Gui/ALL/all_export_as.png) (Export).  
     
   ![](../Pictures/Gui/ALL/arrow.png) By default the current project name is preset as the name of the configuration file to be exported in the File name field.
2. If desired, change the target directory and the name of the file to be exported and click [Save].

### Import the layer management configuration

1. In the Layer management - <Project name> dialog click ![](../Pictures/Gui/ALL/all_import_as.png) (Import).
2. If necessary, navigate to the directory containing the desired configuration file (\*.elc). Select the file and click [Open].  
     
   ![](../Pictures/Gui/ALL/arrow.png) If the configuration file contains user-defined layers that are not present in the current project, these layers are created. User-defined layers that exist in both the configuration file and in the current project are reconfigured according to the settings in the configuration file.

See also

[Layer Management](layermanager_k_start.htm)

[Layers: Principle](layermanager_k_prinzip.htm)

[Working with Layers](layermanager_h_ebenenbearbeiten.htm)

[Dialog Layer management - <Project name>](layermanager_d_ebenenverwaltung.htm)