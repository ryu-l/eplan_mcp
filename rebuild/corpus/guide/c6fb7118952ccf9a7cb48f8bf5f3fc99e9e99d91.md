# Defining Net Extensions for Numbering with PLC Data

Typically a net ends at terminals, pins, or connection points of functions. You can use the net extension function to extend the net with terminals and pins, for example. To do this, specify in the net extension dialog which functions should not be treated as a boundary for the net. This setting affects the designation if the range is selected as "Net". The net extension for numbering with PLC data is independent of the Net-connecting property of the function concerned.

Precondition:

You have opened a project.

1. Select the following commands: File > Settings > Projects > "Project name" > Device > Number using PLC data.
2. Select a user-defined scheme from the drop-down list, or create a new scheme via ![](../Pictures/Gui/ALL/all_new_as.png) (New) in the dialog Settings: Number using PLC data.
3. Click [Net extension].
4. In the Net extension dialog use the arrow button to move the desired functions from the Selection list into the Net extended with following objects list.  
     
   ![](../Pictures/Gui/ALL/arrow.png) The net then no longer ends at the selected functions; it is now extended beyond them.  
     
   ![](../Pictures/Gui/ALL/arrow.png) The net extension applies to all format groups with the scope "Net".
5. Click [OK].  
     
   ![](../Pictures/Gui/ALL/arrow.png) The setting is saved in the selected scheme.

See also

[Dialog Net extension (Numbering with PLC Data)](offlinenumerationplcgui_d_netzerweiterung.htm)

[Determining the Format and Range for Numbering](offlinenumerationplcgui_h_formatfestlegen.htm)