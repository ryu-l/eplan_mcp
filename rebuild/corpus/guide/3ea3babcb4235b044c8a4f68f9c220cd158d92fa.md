# Dialog "SQL server setting" / Dialog "Generate SQL server database"

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Calling up dialog SQL server setting:](javascript:void(0);)

- File > Settings > User > Translation > Dictionary. Select the SQL server option and click [...].
- Tab Tools > Command group Translation > Dictionary. Select the Management tab. Select the SQL server option and click [...].
- File > Settings > User > Management > Project management database. Select the SQL server option and click [...].

In this dialog, you define the default settings for the SQL server that is to be used when working with the dictionary or project management.

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Calling up dialog SQL server setting (Parts Management):](javascript:void(0);)

- Tab Master data > Command group Parts > Management. Button [Extras] > Settings. Select the SQL server option and click [...].
- File > Settings > User > Management > Part. Select the SQL server option and click [...].
- You have opened a project and selected a main function in the graphical editor or in a navigator. Popup menu item Properties. You clicked [...] in the Parts tab next to the Data source field. In the dialog Settings: Parts (user) select the SQL server option, and click [...].

In this dialog, you define the default settings for the SQL server that is to be used when working with parts management and / or part selection. These settings are exclusive, i.e. settings made for the parts management do not automatically apply to the part selection.

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Calling up dialog Generate SQL server database:](javascript:void(0);)

- Tab Tools > Command group Translation > Dictionary. Select the Management tab. Select the SQL server option. [Extras] > New.
- File > Settings > User > Management > Project management database. Select the SQL server option. Click ![](../Pictures/Gui/ALL/all_new_as.png) (New).
- Tab Master data > Command group Parts > Management. Button [Extras] > Settings. Select the SQL server option. Click ![](../Pictures/Gui/ALL/all_new_as.png) (New).

In this dialog you can generate a new, still empty, SQL server database.

![](../Pictures/Gui/ALL/note.png)Note:

As for the conversion of the dictionary to an SQL server, the Eplan platform supports the versions of the Microsoft SQL server approved in the performance description. This also applies for conversion of the parts database and for setting of the internal part selection to an SQL server.

Overview of the main dialog elements:

Server:

Here you define the name of the server on which the databases are managed.

Sign-in:

Select the type of sign-in used for the SQL server from this drop-down list:

- Windows authentication: This setting allows you to access the SQL server as soon as you have signed in as a Windows user. A separate password for the SQL server is then not required.
- SQL server: With this setting, you can only access the SQL server after providing user authentication in the two fields below. To do this, an appropriate access rights database must be created and filled with data on the SQL server.

User / Password:

These fields are only active when you have selected the SQL server option under Sign-in!  
Here you enter the name and password to be used when access to the SQL server is to be password-protected. The characters entered are replaced with periods in order to preserve secrecy.

Database:

Store here the name of the database that is to be used for the dictionary or project management database. Or select the name of a database existing on the specified server from the drop-down list.

Connection test:

Click this button to check the connection to the set SQL server. In the process, for example, the connection to the server and to the master SQL database is checked. This way you can identify a possible cause faster in case of problems when using SQL databases. In the subsequent SQL connection test dialog you can see whether the test was successful and the connection could be established in the Status and Status message columns. If the test is successful, the respective version of the SQL driver and of the SQL server are displayed.

![](../Pictures/Gui/ALL/info.png)Tip:

If you wish to transfer the data of the dictionary or the parts management from an existing internal Eplan database to an SQL server database, first export the data from the dictionary or the parts management in the XML file format, generate a new SQL server database, and then import the data into the new database.

See also

[The Dictionary](translatedbgui_k_start.htm)

[Automatic / Manual Translation](translategui_k_start.htm)

[Selecting a Project Management Database](prjmanagementgui_h_projekteverwalten.htm)

[Parts Management](articlesgui_k_start.htm)