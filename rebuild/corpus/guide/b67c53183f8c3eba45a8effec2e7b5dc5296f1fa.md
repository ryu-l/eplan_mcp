# Placing Connections

By means of connection definition points it is possible to place unplaced connections or to place already placed connections again.

1. Select the following commands: Tab Connections > Command group Connections > Navigator.
2. Select the connections you wish to place. Multiple selection is possible.
3. Select the Place popup menu item.
4. Place the connection definition point on an autoconnect line.  
     
   ![](../Pictures/Gui/ALL/arrow.png) The connection data is assigned to the connection definition point. Unplaced connections are deleted after the placement.

### Delete connection placements

A connection placement can only be deleted via its connection definition point. The connection will then exist as an unplaced connection in the project.

1. Select the desired connection definition points.
2. Select the following commands: Tab Home > Command group Clipboard > Drop-down button Delete > Placement.  
     
   ![](../Pictures/Gui/ALL/arrow.png) The connection definition points in question will disappear and unplaced connections will appear. This also applies if the connection it is associated with does not disappear. The result will be two connections: one placed without special properties and one unplaced with special properties.  
     
   ![](../Pictures/Gui/ALL/arrow.png) If an unplaced connection already exists with identical targets, an additional unplaced connection is added. Because multiple connection definition points can be located on the same connection, this can result in inconsistencies.  
     
   ![](../Pictures/Gui/ALL/arrow.png) If you delete the placements at several connection definition points of one connection, several unplaced connections will be the result. You can use or place these unplaced connections again on multiple connection definition points of one connection, in which case you again have the same result as before deleting the placements.

![](../Pictures/Gui/ALL/note.png)Notes:

- If placements from connected components are deleted, the associated connections only remain as unplaced if the connection definition points were marked (that is, if their placements were also deleted). If the placements of the connection definition points are not deleted, no unplaced connections will result, but the connections will be completely deleted.
- You can only delete a placement if the connection definition point is uniquely assigned to one individual connection. If this is not the case, then an appropriate message is displayed.

See also

[Unplaced Connections](connectionbrowsergui_k_npv.htm)

[Updating Connections](connectionbrowsergui_h_verbindungenbearbeiten.htm)

[Drawing Connection Definition Points](connectionbrowsergui_h_definitionspunktezeichnen.htm)

[Defining the Connection Properties](connectionbrowsergui_h_verbindungseigenschaftendefinieren.htm)

[Viewing and Editing the Connection Properties](connectionbrowsergui_h_eigenschaftenbearbeiten.htm)