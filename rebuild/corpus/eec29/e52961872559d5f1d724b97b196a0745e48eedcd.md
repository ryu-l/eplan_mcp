# Expanding the configuration with nodes for room components

The nodes for room components must be configured in such a way that the corresponding image, suitable for the type of room component, is inserted in the diagram. To that end, there are images in an image register assigned to the types of room components.

More:

[Configuration of the nodes for room components](tutgraph2d_h_configuration_of_nodes_for_roomcomponents.htm)