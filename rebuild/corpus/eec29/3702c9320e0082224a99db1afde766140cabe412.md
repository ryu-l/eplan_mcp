# Terminal

The terminal represents the physical signal level and constitutes the link between a communication channel and the signal. This object must contain parameters for address generation and must implement the ITerminal interface if the initial addresses have to be calculated via the IO generator.