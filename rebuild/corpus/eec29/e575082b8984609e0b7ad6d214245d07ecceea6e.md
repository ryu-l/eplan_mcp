# Replacement for <null> values

Path to model variable:

Disciplines > ECAD > EPLAN Electric P8

Specify a value. During the generation of the schematics, the value replaces all <zero> values so that the process is not interrupted.