# Toolbars

The EEC Workbench contains a global toolbar **(1)**, which is used to execute global actions, for example, full-text search, and a view-specific toolbar (2), for example, to create the schematic for the project (Pfuderer\_GolfBallMachine\_P8) displayed in a view.

![](../Pictures/eec_werkzeugleisten.png)