# Extension point: Term definition

[Open topic with navigation](../../EPLAN_Help.htm#htm/glossary_o_extension.htm)

### Extension point

Modeling

Extension points are placeholders, which implement an interface. Components of a variants library, which contain the identically named interface, are instantiated on the extension point. The instantiation, updating or deleting of components on a extension point has to be initiated manually or by scripting methods.