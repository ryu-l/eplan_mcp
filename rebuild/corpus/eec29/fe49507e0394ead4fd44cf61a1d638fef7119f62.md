# propertyNames(String sqlStatement)

Returns the names of the properties which are contained in a data source object query.

| **propertyNames([String](glossary_o_string.htm) sqlStatement)** | | | |
| --- | --- | --- | --- |
| Argument | String | sqlStatement | SQL query to the data source |
| Return value | String |  | |

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example code:](javascript:void(0);)

Creating a map where the keys are the property names and the values are the column content:

```
=type('Pfuderer-Baukasten.Stückliste.ArtikelDB').content().intoInject
(
	record,map | record.propertyNames.intoInject
	(
		propertyName,innerMap | 
		if not innerMap.containsKey(propertyName)
		then innerMap.add(propertyName,List {record.property(propertyName)})
		else innerMap.add(propertyName,innerMap.value(propertyName).append
		(record.property(propertyName)))
		endif,map
	) , Map{}
)
```