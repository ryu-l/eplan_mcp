# treeNotifications

The <treeNotifications> element is only implemented in a tree structure when a trigger event occurs that is supposed to execute an action, e.g. when a user clicks a project component.

This element has no attributes.

| Allowed sub-elements | Quantity |
| --- | --- |
| [onClickEvent](refformui_r_onclickevent.htm) | 1 |