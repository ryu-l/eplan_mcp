# Local variables

In formulas variables are defined by means of the keyword let. These are to be used instead of the expressions which they represent. This improves the overview for complex formulas.

More:

[let](refformulas_r_local_variable_let.htm)