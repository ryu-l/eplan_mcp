# Toolbar with Tools for Creating new Instances

The palette of the Graph-UI contains a section Components (see the following figure), that contains tool for instantiation of components in the diagram.

![](../Pictures/diagram_eo_erzeugungswerkzeuge.png)

To instantiate a component, first the tool is to mark in the palette. Then instances are created by mouse clicks in the diagram.