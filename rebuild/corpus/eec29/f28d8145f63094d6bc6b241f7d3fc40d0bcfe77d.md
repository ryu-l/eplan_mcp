# Create parameter units on demand

Path to model variable:

Disciplines > ECAD

Select a [Boolean](glossary_o_boolean.htm) value. The value determines whether units in which the referenced parameters are stored are created when ECAD resources are added.

Valid values are true and false.