# Methods of the UserMessageCollector no longer supported

```
addDebug(...)
```

Instead of addDebug(â¦), debug(...) of the SpaceLogger can be used.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example code:](javascript:void(0);)

```
import SpaceLogger (TODO)
[...]
SpaceLogger sp = SpaceLogger.getSpaceLogger("MyLogger");
sp.debug("Information for the log.");
```