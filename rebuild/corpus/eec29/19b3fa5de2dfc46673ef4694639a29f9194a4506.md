# Create parameter units on demand

Path to model variable:

Disciplines > PLC > AutomationWorX

Select a [Boolean](glossary_o_boolean.htm) value. The value determines whether missing parameter units automatically created when AutomationWorX resources are added.

Valid values are true and false.