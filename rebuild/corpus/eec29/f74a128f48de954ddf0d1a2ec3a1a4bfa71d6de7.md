# Scalable background image

A scaling factor can be specified for the background image of a diagram. By means of the popup menu, menu item Set Background Image, the dialog Background Image Settings is opened (see the following figure).

![](../Pictures/diagram_hintergrundbild_einstellungen.png)

The slider Scaling is used to adjust the size of the background image.