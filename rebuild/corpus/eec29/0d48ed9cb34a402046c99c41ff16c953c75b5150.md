# LOOP

LOOP iterates over an object list. The list of objects is created by a formula, that returns a list (e.g. dos). Method calls within LOOP will be sent to the object from the object list. The control structure ends with END\_LOOP.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

```
(*{LOOP:dos}*)
(*{name}*)
(*{END_LOOP}*)
```

Within LOOP can also be used an index variable. Method calls within LOOP with index variable will be sent to the object that is connected to the text. For Method calls for objects from a object list, the index variable is used.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

```
(*{LOOP:x|List{0..10}*)
Name des Objekts auÃerhalb von LOOP=(*{name}*)
Name des Objekts an Position ({x+1})=(*{dc.dos.at(x).name}*)
(*{END_LOOP}*)
```