# Checking for errors using the UserMessageCollector

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example code in Groovy:](javascript:void(0);)

```
import org.foederal.util.ui.messages.UserMessageCollector;
logText1 = "Abort script caused by errors.";
[...]
[... First part - e.g. create something ...]
if (UserMessageCollector.hasErrors())
{
	UserMessageCollector.addInfo (LIBRARY, self, null, "Script ", logText1);
	return;
}
[... Second part - e.g. create something with the result of the first part ...]
```