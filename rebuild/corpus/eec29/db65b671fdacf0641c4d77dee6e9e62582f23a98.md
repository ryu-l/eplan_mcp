# Component SapObject

The component SapObject is used as a wrapping object to call up one or more function modules. These can be in parallel, separated from each other in time, or can be mutually dependent. In addition to the parameter Destination, all necessary parameters for a module to be activated in SAP are applied.