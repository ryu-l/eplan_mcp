# Remove prefixes from variable names

Path to model variable:

Disciplines > PLC

Select a [Boolean](glossary_o_boolean.htm) value. The value determines whether the prefixes are removed from the variable names during the generation of the PLC code.

Valid values are true and false.