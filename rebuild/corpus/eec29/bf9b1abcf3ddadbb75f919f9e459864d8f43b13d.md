# Back-end title

Path to the preferences:

Menu Window > Preferences

Dialog Preferences > Back-end title

- The Show the back-end used in the window title option activates the display of the absolute path to the back-end in the header of the EEC window.

The option is active as a default.