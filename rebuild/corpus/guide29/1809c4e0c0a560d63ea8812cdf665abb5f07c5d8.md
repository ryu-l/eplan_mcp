# Addressing PLC Connection Points

[PLC connection points](Glossary_o_sps_anschluesse.htm) can be manually, subsequently, or automatically addressed.

- With manual addressing you assign the PLC address to each individual connection point. To do so enter the address in the Address field of the PLC connection point tab of the [property dialog](Glossary_o_eigenschaftendialog.htm).
- With subsequent addressing, you assign new addresses to multiple already addressed PLC [connection points](Glossary_o_anschluesse.htm) in a single step. Select the menu [items](Glossary_o_bauteile.htm) Project data > PLC > Address to this purpose.
- With automatic addressing, PLC connection points are automatically assigned a new address when they are inserted into a schematic or overview page.

If you are using different PLC controllers in a project, each CPU should be assigned a scheme for the PLC-specific [settings](Glossary_o_einstellungen.htm). In such a scheme the [formats](Glossary_o_formate.htm) for the [PLC addresses](Glossary_o_sps_adressen.htm) are defined in relation to the PLC type. In the case of subsequent or automatic addressing, the settings from the respective scheme as assigned to the CPU are taken into account.

If no scheme for the PLC-specific settings has been assigned to a CPU, the scheme from the project settings is used. When automatically addressing different CPUs without assigned scheme, incorrectly formatted addresses may be suggested.

If you insert PLC connection points by means of symbol or macro selection or copy PLC connection points, the scheme from the project settings is also used for automatic addressing.

If you work in [multi-user operation](Glossary_o_multiuser_betrieb.htm), we recommend disabling automatic addressing of PLC addresses.

![](../Pictures/Gui/ALL/note.png)Note:

If the entered start address and the address format do not agree during subsequent addressing, a message is displayed. In this case, you must adjust the start address and / or the address format.

### PLC Connection Points with Fixed Addresses

If you use PLC connection points with fixed addresses in your project, these should normally not be modified during subsequent addressing or during the insertion of [macros](Glossary_o_makros.htm). With the property Do not include in addressing you can specify at a PLC connection point that it is to be excluded from addressing and thus always retain its original address.  
During addressing such PLC connection points are also not displayed in the result preview.

Ensure that the property is set at every used representation type of the PLC connection point so that the [representation types](Glossary_o_darstellungsarten.htm) behave the same during addressing.

### PLC cards with different address ranges

You can specify two separate start addresses for the inputs and outputs for [PLC cards](Glossary_o_sps_karten.htm) that have both inputs and outputs. To this purpose the [properties](Glossary_o_eigenschaften.htm) Start address of PLC card and Start address 2 of PLC card are available in the PLC box tab of the property dialog. These properties can be used when addressing with [configuration value](eservicesgui_d_formatzaehler.htm#Konfigurationswert).

![](../Pictures/Gui/ALL/info.png)Tip:

The [check runs](Glossary_o_prueflaeufe.htm) 004106 and 004107 are available to check whether addresses are located outside the address range of the PLC card or whether address ranges of multiple PLC cards overlap. If you want to use these check runs, the properties PLC device: Data length (inputs) and / or PLC device: Data length (outputs) must also be filled in addition to the start addresses. The address range is determined from the start address and the data length. The data length indicates the number of bits up to the next free address on the next PLC card.

In addition you have the possibility to manage PLC [devices](Glossary_o_betriebsmittel.htm) that consist of several integrated [modules](Glossary_o_module.htm) and have several different address ranges with different start addresses. To this purpose, up to twelve PLC subdevices are available that are defined by the corresponding properties.

See also

[Address Formats](plcprocessorgui_k_adressformate.htm)

[Defining Address Formats](plcprocessorgui_h_adressenformateeinstellen.htm)

[Automatically Assigning Addresses to PLC Connection Points](plcgui_h_automatisch_adressieren.htm)

[Dialog Readdress PLC connection points](plcgui_d_anschluesseneuadressieren.htm)

[Tab PLC box](devicetaggui_r_spskarte.htm)

[PLC Subdevices](plcgui_k_untergeraete.htm)