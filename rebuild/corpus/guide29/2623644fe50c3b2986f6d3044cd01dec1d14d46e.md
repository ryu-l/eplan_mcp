# P026075: Bending radius of the routing connection is too small

### Cause

The bending radius of the routing connection between two control points is smaller than the minimally permitted bending radius stored in parts management for the routing connection (tube / pipe).

### Solution

1. Locate the routing connection in the [layout space](Glossary_o_bauraum.htm) using the Go to (graphic) functionality from the popup menu in the Message management dialog.
2. Change the bending radius of the routing connection in the layout space so that it corresponds at least to the value of the minimally permitted bending radius as defined for the routing connection in parts management, or is even greater.
3. Route the [connections](Glossary_o_verbindungen.htm) again, then start a new check run.