# Tab Net connections

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Dialog call:](javascript:void(0);)

You have opened a project.

- You have selected a net definition point in the graphical editor. Edit > Properties. In the Properties <...> dialog, select the Net connections tab.
- Insert > Net definition point. In the Properties <...> dialog, select the Net connections tab.

You define the net [connections](Glossary_o_verbindungen.htm) in this tab. A net consists of [connection points](Glossary_o_anschluesse.htm) that are connected directly to each other and the connections between these connection points.

Overview of the main dialog elements:

Table Connection points

All the connection points of the net and the following data are shown in this table:

- Connection point: The connection point that is the source of the connection
- Symbol connection point: A unique connection point number for the symbol. These numbers enable you to distinguish between connection points if you have not issued [connection point designations](Glossary_o_anschlussbezeichnungen.htm).
- ID for net-based connections: Unique connection point [ID](Glossary_o_id.htm)
- Number of targets: Number of targets for this particular connection point.

If, in the project [settings](Glossary_o_einstellungen.htm) (Options > Settings > Projects > "Project name" > Connections > General), you selected the Manage saddle jumper connection points in the net definition point check box, symbol connection points with the connection point type "Saddle jumper" are also displayed here. However, additional function connection points for saddle jumpers are not displayed.

If the Manage all the net connections in the net definition point check box is selected in the project settings, the targets are not automatically specified for net-connecting [functions](Glossary_o_funktionen.htm) that are directly connected to one another. In this case the connections are not automatically generated and have to be specified manually.

![](../Pictures/Gui/ALL/note.png)Notes:

- Net based connections cannot be used together with distributed terminal jumper optimization. The two functionalities [generate](Glossary_o_erzeugen.htm) different connections that are incompatible with one another.
- If there is a connection symbol with target specification between the net-connecting functions (e.g. a [T-node](egedgui_d_tstueck.htm)), the connections are specified automatically in all cases.

Toolbar:

| Button | Meaning |
| --- | --- |
| (Create daisy chain) | Enables a daisy chain to be created. Every connection point that is then clicked on will be added to the daisy chain. |
| (End daisy chain) | Ends the daisy chain. |
| (Edit) | Allows the connection point ID (ID for net-based connections property) for the currently selected connection point to be edited. |

Popup menu:

| Menu item | Meaning |
| --- | --- |
| Create daisy chain | Enables a daisy chain to be created. Every connection point that is then clicked on will be added to the daisy chain. |
| End daisy chain | Ends the daisy chain. |
| Changing the target order | Opens the Target order dialog. The order of the targets for the selected connection point can now be modified. Only the targets of connections that are stored in the net definition point are listed. If the net also contains connections that are defined using connectors with target specification, the target order for these is specified by the connectors and cannot be modified. |
| Properties | Allows the connection point ID (ID for net-based connections property) for the currently selected connection point to be edited. |

---

Table Connections

In this table, each daisy chain is displayed as a list of connection points. The connection point and the ID for [net-based connections](Glossary_o_netzbasierteverbindungen.htm) are displayed as standard. You can also display the [properties](Glossary_o_eigenschaften.htm) of the connections. Every connection point in a daisy chain has a connection to the connection point above and below it in the list. This means that all the connection points in the middle of a daisy chain have two connections, whereas the connection points at the beginning and end of a daisy chain have just one.

![](../Pictures/Gui/ALL/info.png)Tip:

You can modify the order of the source and target of a connection stored in a net definition point by activating the Exchange source and target property in the properties dialog for that connection.

Toolbar:

| Button | Meaning |
| --- | --- |
| (Edit) | Opens the [property dialog](Glossary_o_eigenschaftendialog.htm) for the connection between the selected connection point and the one following it in the list so that the connection properties can be edited. |
| (Delete) | Removes the selected connection points from the table. This also changes the connections. Please note that there is no prompt for confirmation! |
| (Move up / Move down) | Moves the selected connection points up / down, if possible. This also changes the connections. |

![](../Pictures/Gui/ALL/info.png)Tip:

You can select the complete daisy chain by clicking on the [separator](Glossary_o_trennzeichen.htm) underneath it. This allows you to move the entire daisy chain using the arrow buttons or to delete it using the ![](../Pictures/Gui/ALL/all_delete_as.png) button.

Popup menu:

| Menu item | Meaning |
| --- | --- |
| Other connections | Opens the Other connections dialog, which displays the other connections in the net that are not stored in the net definition point. These include connections that are defined using connectors with target specification or via other [net definition points](Glossary_o_netzdefinitionspunkte.htm). Connections that are automatically generated between net-connecting functions that are connected directly to one another are displayed here too, providing the Manage all the net connections in the net definition point check box is deselected in the project settings. |
| Properties | Opens the property dialog for the connection between the selected connection point and the one following it in the list so that the connection properties can be edited. |

See also

[Defining Daisy Chains](netbasedwires_h_verbindungsketten.htm)

[Definition of Potentials and Signals](potentialbrowsergui_k_prinzippotenziale.htm)

[Dialog Settings: General (project, connections)](connectionsettingsgui_d_einstellungenallgemein.htm)