# Dialog Settings: Image file export

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Dialog call:](javascript:void(0);)

- Page > Export > Image file. Next to the Scheme field, click [...].
- Options > Settings > Company > Interfaces > Image file export.

In this dialog you define the [settings](Glossary_o_einstellungen.htm) for exporting image files and save this as a scheme. The [schemes](Glossary_o_schemata.htm) for image file export are saved on a per-company basis. You can copy and edit an existing scheme or [create](Glossary_o_erstellen.htm) a new scheme.

The following dialog elements are available:

Scheme:

Select a scheme to define the settings from the drop-down list.

You can create new schemes, save any changes to a scheme, and copy or delete schemes via the adjacent [Toolbar](schemeconfig_m_schemaschaltflaechen.htm). Schemes can also be imported or exported.

Description:

This field displays the descriptive text that you entered into the Description field in the New scheme dialog when you created the scheme.

Target directory:

Enter the directory where the image files are to be saved. Every page is saved as a separate image file. Use [...] to open the dialog for choosing a directory.

You can use the Insert path variable popup menu item to branch to the dialog [Select path variable](modaldialogsdb_d_pfadvariablen.htm) where you can select one of the available [path variables](Glossary_o_pfadvariablen.htm).

File type:

Use this drop-down list to select the desired image format. BMP, GIF, JPG, PNG, and TIFF are possible [formats](Glossary_o_formate.htm).

Compression:

Depending on the chosen file type, this drop-down list offers various [levels](Glossary_o_etagen.htm) of compression. Select the desired level of compression.

You will find detailed information on the level of compression for the TIFF file type online at "http://partners.adobe.com/public/developer/en/tiff/TIFF6.pdf".

Color depth:

Depending on the chosen file type, this drop-down list offers a range of different color depths. The color depth allows you to define how many colors can be represented in the exported image file. The color depth is defined as the "number of bits per pixel" (e.g.: 1 bit per pixel means 2 colors, 4 bits per pixel 16 colors, 8 bits per pixel 256 colors, etc.).

Width:

This field allows you to define the desired width of the image file in pixels. The appropriate height is calculated by EPLAN based on the aspect ratio of the page. This setting has the effect of making all exported pages the same width, regardless of their format (e.g. DIN A4 or DIN A0).

See also

[Dialog Image file export](modaldialogsdb_d_export.htm)

[Outputting Pages as Image Files](gedgraphicexportgui_h_seitenexportieren.htm)