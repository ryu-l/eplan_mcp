# Dialog Use case

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Dialog call:](javascript:void(0);)

Utilities > Parts > Management. You have highlighted a [part](Glossary_o_artikel.htm) in the tree or list, from the generic product group "Electrical engineering" or "Fluid power". Select the Safety-related values tab.

- Click ![](../Pictures/Gui/ALL/all_new_as.png) (New) on the toolbar above the Use cases list.
- Select an entry in the Use cases list, and click ![](../Pictures/Gui/ALL/all_edit_as.png) (Edit).

The usage of a device is known as a so-called "use case". Each use case has its own set of safety-relevant characteristics, the "[safety-related values](Glossary_o_sicherheitskennwerte.htm)". This means that, depending on how a device is used, differing safety-related values must be used. Differing uses cases may be derived, for example, from the differing use of a device. Thus, for example, a drive can be operated with or without brakes, which in turn means differing safety-related values.

The [use cases](Glossary_o_anwendungsfaelle.htm) can be structured in up to five hierarchical [levels](Glossary_o_etagen.htm). There must be at least one hierarchical level. The safety-related values are located at the lowest hierarchical level. If use cases have hierarchical levels with the same designation, this creates a tree-like structure.

Use this dialog to [create](Glossary_o_erstellen.htm) or edit use cases.

Overview of the main dialog elements:

Designation:

Store in this field the designation of the use case.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

For example, the probability that a device that is operated outdoors in all kinds of weather fails is certainly higher than that of a device that is operated in an air-conditioned room. The use cases of "outdoors in all kinds of weather" and "in an air-conditioned room" are then differing uses cases.

Below the highlighted hierarchy level:  
At the same hierarchy level:

These two options will not be available until at least one use case has been defined.  
Select the Below the highlighted hierarchy level option if the new use case is to [generate](Glossary_o_erzeugen.htm) a hierarchy level below the currently highlighted one. (There is a maximum of five hierarchy levels.)   
Select the At the same hierarchy level option if the new use case is to be generated in parallel to the currently highlighted one.

See also

[Dialog Parts management](partsmanagementgui_d_artverw.htm)

[Dialog Import records](partsmanagementgui_d_artikelimport.htm)

[Tab Safety-related values](partsmanagementgui_r_sicherheitskennwerte.htm)