# Tab Block definition

[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Dialog call:](javascript:void(0);)

- Options > Settings > User > Interfaces > DXF / DWG export and import. Select the Block definition tab.
- Page > Export > DXF / DWG. Next to the Scheme field, click [...]. Select the Block definition tab.

In this tab you define which block are to be taken into consideration for the export.

Overview of the main dialog elements:

Group box Dynamic reports

For exports to [DXF](Glossary_o_dxf.htm) / [DWG](Glossary_o_dwg.htm) format the dynamic and conditional areas of the [forms](Glossary_o_formulare.htm) are combined into [blocks](Glossary_o_bloecke.htm) and transferred. Specify the prefixes of the [designations](Glossary_o_bezeichnungen.htm) for each area here, for example "HEADER" for the header area.

---

Group box Block properties

In this group box you can specify the following [settings](Glossary_o_einstellungen.htm):

- Export invisible attributes: Select this check box if all AutoCAD-specific attributes for an item should be exported, including the invisible ones.
- Generate one block per symbol: Select this check box if an AutoCAD block should be generated for every placed EPLAN symbol. If the check box is deselected, no blocks will be generated and the pages will be exported as pure graphics.
- Generate a block for plot frames: Select this check box if [AutoCAD blocks](Glossary_o_autocad_bloecke.htm) should be generated for all EPLAN [plot frames](Glossary_o_normblaetter.htm). If the check box is deselected, no blocks will be generated and the plot frames will be exported as pure graphics.
- Generate a block for standard forms: Activate this check box if AutoCAD blocks should be generated for all EPLAN standard forms. If the check box is deselected, no blocks will be generated and the forms will be exported as pure graphics.

See also

[Dialog Settings: DXF / DWG export and import](xdxfgui_d_einstellungen.htm)

[Dynamic Forms](formeditorgui_k_dynamformulare.htm)