# Editing Connection Points

Precondition:

You have opened a project.

1. Utilities > Master data > Symbol > Open
2. Select the menu [items](Glossary_o_bauteile.htm) Utilities > Number connecting points.
3. In the Sequence group box specify whether the [connection points](Glossary_o_anschluesse.htm) should be numbered according to the "from top to bottom and left to right " method or the "from left to right and top to bottom" method.
4. Click [OK].
5. Select the menu items Insert > Connection point "Direction".
6. Place the connection point.  
    Or, before placement, press [Ctrl] and move the mouse to switch back and forth between the [connection point directions](Glossary_o_anschlussrichtungen.htm).  
    Or press the [TAB] button to rotate the connection point counter-clockwise through the possible directions.
7. In the Connection point number field in the Connecting point dialog specify the connection point number (or use the "Next free number" default).
8. Select the desired Connection point direction from the drop-down list.
9. From the drop-down list on the left select one or more of the property sets (Connection point designation and Connection point description).
10. Specify the [settings](Glossary_o_einstellungen.htm) for the corresponding texts in the field on the right of the dialog.
11. Click [OK].

![](../Pictures/Gui/ALL/warning.png)Warning:

Multiple symbol connection points can't be located on top of another.

Connection point data will be applied according to your settings for the symbol. The graphic for the connection point will indicate the connection point direction explicitly, but later on it is not an integral element of the symbol.

![](../Pictures/Gui/ALL/note.png)Notes:

- The menu items View > Properties as numbers displays the [property texts](Glossary_o_eigenschaftstexte.htm) (device tags, function text, etc.) to be switched in order to make the display clearer. If you print the current page with this setting or export it as a PDF, the numbers, not the property texts, are output in the print-out / PDF export file.
- To display and switch between the Connection point designation / Connection point description property sets, use the menu items View > Non-rotated connection point designations or View > Rotated connection point designations.

See also

[Dialog Connecting point](symboleditorgui_d_anschlusspunkt.htm)