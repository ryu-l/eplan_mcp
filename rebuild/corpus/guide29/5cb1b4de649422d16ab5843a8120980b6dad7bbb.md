# EPLAN Fluid: Trades

In EPLAN Fluid, it is possible to [generate](Glossary_o_erzeugen.htm) [reports](Glossary_o_auswertungen.htm), and filter and sort them by trades. EPLAN Fluid differentiates between the following [trades](Glossary_o_gewerke.htm):

- Electrical engineering
- Hydraulics
- Pneumatics
- Cooling
- Lubrication
- Cooling lubricant
- Gas technology
- Fluid power (undefined).

For example, separate connection lists and / or [bills of materials](Glossary_o_stuecklisten.htm) can be output for hydraulic and pneumatic components by specifying the trade.

You specify the trade either when creating a new function in the navigator or when [placing](Glossary_o_auflegen.htm) a function in the schematic. You specify the setting in the Properties <...> dialog of the respective function. The Properties table in the <Function category> tab can be extended to include the Trade drop-down list for this purpose.

EPLAN Fluid automatically uses the last trade setting selected as the default for a newly created or newly placed function.

If you assign a new definition that you select from the general [functions](Glossary_o_funktionen.htm) area to a placed function, the page type determines the default trade for the function. On a "[fluid power](Glossary_o_fluidtechnik.htm) schematic"-type page, the fluid power-specific trades are then automatically selectable, while on a "schematic multi-line"-type page, the "Electrical engineering" trade is the default.

![](../Pictures/Gui/ALL/note.png)Note:

In the fluid schematic, if you place [symbols](Glossary_o_symbole.htm) from the fluid power [symbol libraries](Glossary_o_symbolbibliotheken.htm) for electromagnetic actuation of valves (solenoids), then the function definition is used to automatically set the trade to "electrical engineering".

The trade can also be configured for project parts and [mounting panels](Glossary_o_montageplatten.htm).

Conversely, connection definition points of the trade can't be configured. This property is automatically copied from the connected functions, i.e. the targets of the connection.

### Subtrades

In addition to trade [settings](Glossary_o_einstellungen.htm), "[subtrades](Glossary_o_untergewerke.htm)" can be defined for functions in EPLAN Fluid. For instance, you can assign the "Water [cooling](Glossary_o_kuehlung.htm)" and "Oil cooling" subtrades to functions of the "Cooling" trade. The definition of subtrades allows you to use very detailed sorting and filter criteria for reports.

Subtrades are also specified in the Properties <...> dialog. The Properties table in the <Function category> tab can be extended to include the Subtrade drop-down list for this purpose. You can choose any [designations](Glossary_o_bezeichnungen.htm) for the subtrade.

### Insert devices with undefined Fluid power trade

In parts management it is possible to assign more than one trade to a [part](Glossary_o_artikel.htm). If you insert a part with such a multiple assignment as a device in the schematic (for example via the Insert > Device menu options), the Select trade dialog is opened. This dialog offers all fluid power trades assigned to the part as well as the additional "Fluid power (undefined)" trade for selection. You must select one of these trades. This trade will then be assigned to the functions of the device.

In the device navigator, e.g., you have the possibility to filter by the "Fluid power (undefined)" trade and then apply a special trade to the corresponding functions.

In tabular editing (menu [items](Glossary_o_bauteile.htm) Edit > In table) you have the additional possibility to assign a specific trade to the functions of [devices](Glossary_o_betriebsmittel.htm) to which the "Fluid power (undefined)" trade has been assigned in a single step.

See also

[Editing Device Properties](devicetaggui_h_bmeigenschaftenbearbeiten.htm)

[EPLAN Fluid: Cross-references to Valve Actuations](ftechnic_k_querverweise.htm)