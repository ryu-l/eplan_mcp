# Inserting Functions

Functions can be added to the project as follows:

- You work with the graphics-oriented interface. In the graphical editor, call up symbol selection and select a symbol. A function definition is defined at the symbol, with which it is inserted. (The symbol and the function are grouped together to make a [component](Glossary_o_schaltzeichen.htm).)
- You insert a macro into the project which contains [functions](Glossary_o_funktionen.htm) (components). A function definition is assigned to the component, or it has a special logic function stored in it.
- You work with the logic-oriented engineering approach and want to work with predefined [devices](Glossary_o_betriebsmittel.htm). You select a device (and thus a [part](Glossary_o_artikel.htm)); this device has data concerning its functions. Now, select one of these functions. All [symbols](Glossary_o_symbole.htm) are offered which are suitable to the display of this function. Now, select a symbol and place it. At the same time, the device is created with all its functions. The information on the functions come in with the part.
- You select (in the navigator) a function (or definition) directly and add it (placed or unplaced) into the project.

### Add functions via symbols

Functions can easily be added by [placing](Glossary_o_auflegen.htm) a symbol on a schematic page.

Precondition:

You have opened a schematic page in the graphical editor.

1. Insert > Symbol
2. Select the desired symbol from the Symbol selection dialog and click [OK].
3. Place the symbol in the schematic.  
      
    ![](../Pictures/Gui/ALL/arrow.png) A function definition is defined at the symbol; this is placed with the symbol as a function.
4. In the Properties <...> dialog, you can edit the [properties](Glossary_o_eigenschaften.htm) of the function.
5. Click [OK].  
      
    ![](../Pictures/Gui/ALL/arrow.png) The new and changed properties are stored in the component and displayed there (as long as they are selected for display).

See also

[Inserting Macros](macrosgui_h_makrosauswaehlen.htm)

[Devices](devicelistgui_k_start.htm)

[Part / Device Selection](partselectiongui_k_start.htm)

[Dialog Function definitions](xfctdefbrowsergui_d_funktionsdefinitionen.htm)

[Changing Functions](xfctdefbrowsergui_h_funktionaendern.htm)

[Swapping Function Definitions](xfctdefbrowsergui_h_funktionsdeftauschen.htm)