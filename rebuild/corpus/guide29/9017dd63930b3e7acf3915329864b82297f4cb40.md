# Working with Grids

You can work with a [grid](Glossary_o_raster.htm) to make the positioning of elements easier. The insertion and element points are then placed on the grid points. A line grid is used for working in a 3D [layout space](Glossary_o_bauraum.htm). In this case, the insertion points are on the points where the [lines](Glossary_o_leitungen.htm) cross.

The size of the grid used (i.e. the clearance between the individual grid points) is stored as a page property and displayed in the status bar. You can select one of the default grid sizes from the drop-down list or enter you own values. With layout spaces, the size of the grid is not a page property.

In the user [settings](Glossary_o_einstellungen.htm), you can define up to five different grid sizes; in the graphical editor, these can then be selected using the ![](../Pictures/Gui/ALL/gededitgui_grida.png) to ![](../Pictures/Gui/ALL/gededitgui_gride.png) buttons in the "View" toolbar. This setting is possible separately for 2D and 3D.

Precondition:

You have opened a page, [form](Glossary_o_verlauf.htm), plot frame or symbol.

### Displaying the Grid

1. Select the View > Grid menu [items](Glossary_o_bauteile.htm) to toggle grid display.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

Schematic section: Grid view off

![](../Pictures/Visualisation/ALL/gededitgui_gridoff_av.png)

Schematic section: Grid view on

![](../Pictures/Visualisation/ALL/gededitgui_gridon_av.png)

### Using Grid Snap

The use of grid snap is independent of the grid display options.

1. Select the menu item Options > Snap to grid, to switch the grid snap on and off.   
     
   ![](../Pictures/Gui/ALL/arrow.png) If the grid snap is activated, then all of the subsequent [actions](Glossary_o_aktionen.htm) are performed within the grid points.  
     
   ![](../Pictures/Gui/ALL/arrow.png) In the status bar, the text "ON" or "OFF" in front of the grid size shows whether the snap to grid is switched on or off.

### Align on grid

This function allows you to subsequently align [objects](Glossary_o_objekte.htm) to the grid settings without having initially drawn them using the grid. If you subsequently switch on the grid snap, then previously drawn objects may no longer be accessible if they aren't on a grid point.

1. Select the objects and then select the Edit > Other > Align on grid menu items.  
     
   ![](../Pictures/Gui/ALL/arrow.png) The selected objects are realigned (i.e. moved) so that all of their important points (such as line starting points, curve ends etc.) are on the grid.

See also

[Graphical Editor](gededitgui_k_start.htm)

[Dialog Settings: 2D (User, Graphical Editing)](gedviewer_d_einstellungenbenutzerallgemein.htm)