# Editing Nets

A net is generated by [placing](Glossary_o_auflegen.htm) the components and connectors so that the [connection points](Glossary_o_anschluesse.htm) of the components are connected by [autoconnecting](Glossary_o_autoconnecting.htm) [lines](Glossary_o_leitungen.htm). If connectors with target specification are used, EPLAN automatically generates the [connections](Glossary_o_verbindungen.htm) within the net. If target specification is disabled, the connections must be defined using a net definition point.

### Change nets

The connections stored in the net definition point are managed using the connection point [ID](Glossary_o_id.htm) (ID for net-based connections property).

If a function within the net is renamed, the connections are retained, as the connection point IDs will not have changed.

If a function is deleted from the net, its connection points and the connections to them remain stored in the net definition point, but are flagged as invalid. The invalid connections must then be deleted â either manually in the [property dialog](Glossary_o_eigenschaftendialog.htm) of the net definition point or automatically when the project is compressed.

If you insert a new function in a net and assign the same connection point IDs as those of the previously deleted function to the connection points, the connections are retained and lead to the new function. If the new function has different connection point IDs, these connection points will not be connected and the net will be incomplete.

### Copy nets

If a net and its net definition point are copied, the connections are also copied. The connection [properties](Glossary_o_eigenschaften.htm) of the copied connections are likewise retained. The source and target of the connections, however, will be invalid if they are not copied as well, e.g. when copying a subnet.

If a macro contains a net definition point, the connections stored in the net definition point will also be placed when the macro is placed.

Copying a net to a location in the schematic where [functions](Glossary_o_funktionen.htm) or connectors already exist can split or extend the net.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

If the net is copied to a location where a net-splitting function exists, the net is split into two [nets](Glossary_o_netze.htm).

![](../Pictures/Visualisation/ALL/netbasedwires_splitnet_av.png)

Connections in the left-hand net:

| Connection point | ID | Connection point | ID |
| --- | --- | --- | --- |
| L1 | 1 | X5:1 | 2 |
| X5:1 | 2 | X5:2 | 3 |
| X5:2 | 3 | X5:3 | 4 |

Connections in the right-hand schematic section:

| Connection point | ID | Connection point | ID |
| --- | --- | --- | --- |
| L1 | 1 | X5:1 | 2 |

Unconnected connection points in the right-hand schematic section:

| Connection point | ID |
| --- | --- |
| -K1:A1 |  |
| -K1:A2 |  |
| X5:2 | 3 |
| X5:3 | 4 |

If the net is copied to a location where a connector is located, the net is extended. The existing connections are retained. Connections to the newly added functions must be manually completed.

![](../Pictures/Visualisation/ALL/netbasedwires_expandnet_av.png)

Connections in the left-hand net:

| Connection point | ID | Connection point | ID |
| --- | --- | --- | --- |
| L1 | 1 | X5:1 | 2 |
| X5:1 | 2 | X5:2 | 3 |
| X5:2 | 3 | X5:3 | 4 |

Connections in the right-hand net:

| Connection point | ID | Connection point | ID |
| --- | --- | --- | --- |
| L1 | 1 | X5:1 | 2 |
| X5:1 | 2 | X5:2 | 3 |
| X5:2 | 3 | X5:3 | 4 |

Unconnected connection points in the right-hand net:

| Connection point | ID |
| --- | --- |
| X5:9 |  |

### Delete net definition points

If a net definition point is deleted, all the connections stored in this net definition point are deleted. The net will then be incomplete. To complete the net again, the missing connections must be defined either in a new net definition point or by using connectors with target specification.

### Move net definition points

If a net definition point is moved to another net, EPLAN attempts to recreate the connections stored in the net definition point using the connection point IDs. If the new net contains connection points with the same IDs as those in the old net, connections will be established between them. IDs that cannot be found in the new net are shown as invalid connection points. The connection points in the old net will be invalid and the net will be incomplete.

If the net definition point is moved back again, the original connections are restored using the connection point IDs.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

The net definition point is moved from the left-hand net to the right-hand one. Some connection points with identical connection point IDs exist in both nets. In the left-hand net X5:1 (ID 2) was connected to X5:2 (ID 3). In the right-hand net X5:5 (ID 2) will be connected to X5:7 (ID 3). L2 and X5:6 have IDs that did not exist in the old net. This means they cannot be connected, so the new net will be incomplete.

![](../Pictures/Visualisation/ALL/netbasedwires_movendp_av.png)

Connections in the left-hand net:

| Connection point | ID | Connection point | ID |
| --- | --- | --- | --- |
| L1 | 1 | X5:1 | 2 |
| X5:1 | 2 | X5:2 | 3 |
| X5:2 | 3 | X5:3 | 4 |

Connections in the right-hand net:

| Connection point | ID | Connection point | ID |
| --- | --- | --- | --- |
| X5:5 | 2 | X5:7 | 3 |
| X5:7 | 3 | X5:8 | 4 |

Unconnected connection points in the right-hand net:

| Connection point | ID |
| --- | --- |
| L2 | 8 |
| X5:6 | 9 |

See also

[Net-based Connections](netbasedwires_k_start.htm)

[Net-based Connections: Principle](netbasedwires_k_prinzip.htm)

[Inserting Net Definition Points](netbasedwires_h_ndpzeichnen.htm)