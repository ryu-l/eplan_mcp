# Using Collision Check

The collision check checks whether [items](Glossary_o_bauteile.htm) overlap or penetrate each other during placement and editing. It always kicks in when items are placed, moved, copied, duplicated, rotated or extended. Resulting collisions are visualized by coloring the affected items. The color used in this context can be set under the Options > Settings > User > Graphical editing > 3D menu items.

- The collision check includes all item types, regardless of whether they are displayed or hidden.
- The collision check can be turned off at any time.
- When [placing](Glossary_o_auflegen.htm) length-variable items, the collision check is activated after defining the first placement point.
- Intentional collisions are not treated as collisions (e.g., a monitor integrated into a door).
- The safety areas (mounting clearances) defined on a [part](Glossary_o_artikel.htm) are evaluated and taken into account by the collision check.
- There is a check run to check for collisions subsequently.

An example is the collision check when inserting a mounting rail.

Preconditions:

- You have opened a project.
- The [layout space](Glossary_o_bauraum.htm) navigator is open and a layout space is open.
- The layout space contains [part placements](Glossary_o_artikelplatzierungen.htm).
- The 3D mounting layout navigator is open.

1. Select the Options > Collision check menu items.
2. Select the Insert > Mounting rail menu items, and select a mounting rail for placement.
3. Set the first placement point, and pull the mounting rail apart in such a way that it touches a part already placed.  
     
   ![](../Pictures/Gui/ALL/arrow.png) As soon as the two items merely touch each other, the snap points are displayed.  
     
   ![](../Pictures/Visualisation/ALL/cabinetgui_collisioncontrol1_as.png)
4. Pull the mounting rail further apart so that it extends over the part already placed.  
     
   ![](../Pictures/Gui/ALL/arrow.png) Both items involved are highlighted in color by the collision check. The collision is reported in the status bar. The second placement point cannot be entered anymore; the placement of the mounting rail is prevented.  
     
   ![](../Pictures/Visualisation/ALL/cabinetgui_collisioncontrol2_as.png)
5. Place the part in a suitable location.

See also

[Dialog Settings: 3D](gedviewer_d_einstellungenbenutzerallgemein3d.htm)

[P026011: 3D graphic of the part placement collides with <x>.](messages_p_026011.htm)