# Net-Based Connections: Principle

Net-based [connections](Glossary_o_verbindungen.htm) enable a schematic to be created without having to define immediately how the connections are arranged. This means that [nets](Glossary_o_netze.htm) containing a large number of connections can be defined within a project. The connection sequence within a net will â assuming [connection symbols](Glossary_o_verbindungssymbole.htm) without target specification are used â not be determined by the connection [symbols](Glossary_o_symbole.htm) in the schematic, but must be defined later in the [property dialog](Glossary_o_eigenschaftendialog.htm) for the relevant net definition point.

### Defining net-based connections

Net-based connections are defined using a net definition point. Which [connection points](Glossary_o_anschluesse.htm) of the net are connected to each other is stored in the net definition point. The connection points are identified by a connection point [ID](Glossary_o_id.htm) that is unique within the net. The IDs are automatically assigned but can be changed manually. These IDs are also copied when a net is copied, so the connections stored in the net definition point are retained.

The connections stored in the net definition point will not be visible immediately in the Connections navigator. Connections are not generated from the information contained in the net definition point until the connections are updated. These connections will then be displayed in the Connection navigator.

If you have a "Net Based Wiring" license, the Specify targets check box will be displayed in the property dialog of connection symbols with branching options (T-nodes, jumpers, double junctions). If this check box is deactivated, the target order for the relevant connection symbol will not be evaluated and the connection symbol will be shown as a point. In this case the connections are not automatically generated and have to be specified manually.

If the Specify targets check box is activated, EPLAN automatically generates the connections.

### Net-connecting functions

A net is terminated by a function that has either potential isolation, signal isolation, or net isolation. Most [functions](Glossary_o_funktionen.htm) have net isolation as standard. The Net-connecting property enables you to specify that the net is to be continued to this function. Potential connection points are always net-connecting.

#### Terminals and pins in nets

It may sometimes be necessary to continue a net via a terminal or a pin. In this case the connections on the two terminal/pin connection points must be [part](Glossary_o_artikel.htm) of the same net. This is achieved by assigning the Net-connecting property to the terminal or pin.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

If the Net-connecting property is assigned to the terminal -X100, the lamps -H10 and -H20 will be part of the same net and can be connected to each other using a net-based connection.

![](../Pictures/Visualisation/ALL/netbasedwires_terminals_av.png)

### Cable definitions

Connections can then only be defined as [cable connections](Glossary_o_kabelverbindungen.htm) via a [cable](Glossary_o_kabel.htm) definition line if the connection does not contain any connection symbols for which target specification is disabled.

![](../Pictures/Gui/ALL/example.png)[![Closed](../../Skins/Default/Stylesheets/Images/transparent.gif)Example:](javascript:void(0);)

The only connection that can be defined as a cable connection using the cable definition line -W2 is the one between -X1:3 and -X1:4. The connection under the cable definition line -W1 will not be recognized as a cable connection even if the connection between X1:2 and X1:1 is defined in the net definition point.

![](../Pictures/Visualisation/ALL/netbasedwires_cable_av.png)

### Net-based connection reports

Net-based connections can be output in [reports](Glossary_o_auswertungen.htm) sorted by nets and [daisy chains](Glossary_o_verbindungsketten.htm). The following [properties](Glossary_o_eigenschaften.htm) can be used for this purpose:

- Net index: Identifies the net in the case of [net-based connections](Glossary_o_netzbasierteverbindungen.htm).
- Daisy chain index: Identifies the daisy chain within a net in the case of net-based connections.
- Connection index: Identifies the connections within a daisy chain in the case of net-based connections.

The Type of administration property can be used as a filter criterion in reports so that only connections with the value "net-based" are output.

See also

[Net-based Connections](netbasedwires_k_start.htm)

[Editing Nets](netbasedwires_k_netzbearbeiten.htm)

[Inserting Net Definition Points](netbasedwires_h_ndpzeichnen.htm)