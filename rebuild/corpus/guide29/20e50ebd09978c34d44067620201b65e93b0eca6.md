# Encoded data: Definition of term

[Open topic with navigation](../../EPLAN_Help.htm#htm/Glossary_o_verschluesseltedaten.htm)

### Encoded data

User interface / operation

Encoded data is not externally readable, but EPLAN can restore the original data from the encoded data. EPLAN can however encrypt new data in the same manner and can use the encryption results to determine if the new data is the same as the original data. An example of encoded data is the password.