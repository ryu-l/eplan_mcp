# Routing Path Type

A routing path type can be assigned to [topology](Glossary_o_topologie.htm) [routing paths](Glossary_o_strecken.htm). The routing path type specifies the way in which [connections](Glossary_o_verbindungen.htm) or cables are routed in this routing path (for example pipe, [cable](Glossary_o_kabel.htm) duct, cable platform, etc.). When [routing connections](Glossary_o_verlegeverbindungen.htm) the [routing path types](Glossary_o_verlegearten.htm) pre-defined at the routing paths are determined for the routed cables and connections and transferred to these.

When a summarized parts list is output, the [functions](Glossary_o_funktionen.htm) can be evaluated by the routing path types, so that the time and costs for routing are calculated.

### Entering the routing path type

The property Topology: Routing path type (routing path) ([ID](Glossary_o_id.htm) 20345) is used to enter the routing path type at topology routing paths. This property specifies the way in which connections or cables are routed in this routing path (for example pipe, cable duct, cable platform, etc.). This results in the routing path type of the connections / cables that are routed in this routing path.

- You can edit the property in the [property dialog](Glossary_o_eigenschaftendialog.htm) of a routing path in the [form](Glossary_o_verlauf.htm) of a selection list. Multilingual input is possible. If you have entered a specific routing path type at a routing path, you can subsequently select these at other routing paths.
- Only one routing path type is permitted per topology routing path.
- This property is available in the topology navigator in the list view and as a filter criterion.
- You can output this property in topology [reports](Glossary_o_auswertungen.htm).

### Output of the routing path type

The property Topology: Routing path types (ID 20344) is available for cables and connections. This property outputs the routing path types of the routing paths that a connection routed in the routing path network or a routed cable runs through. The routing path types are listed separated by a semicolon in the order of the [routing tracks](Glossary_o_verlegewege.htm).

- The property can be displayed in the graphical editor at the function.
- This property is available in the cable and connection navigator in this list view.
- You can output this property in cable reports and connection lists.

See also

[Routing Paths (Topology)](cablinggui_k_start.htm)

[Routing Connections (Topology): Procedure](cablinggui_k_vorgehensweise.htm)