# Logging on or Changing Users

Precondition:

You are using the "Logon" operating mode.

### Log on users

The Log on to EPLAN dialog is automatically displayed when EPLAN starts.

1. Enter the user name and password and click [OK].  
      
    ![](../Pictures/Gui/ALL/arrow.png) EPLAN checks if the user is correctly set up and the password is correct. If this is the case then you are logged on under the specified user name.

### Switch users

1. Close EPLAN via Project > Close.
2. Restart EPLAN.
3. Enter the user name and password into the Log on to EPLAN dialog and click [OK].  
      
    ![](../Pictures/Gui/ALL/arrow.png) EPLAN checks if the user is correctly set up and the password is correct. If this is the case then you are logged on under the specified user name.

![](../Pictures/Gui/ALL/info.png)Tip:

Under Options > Settings > Users > Display > User code / address  you can see which user you are logged on as in the System logon name field.

See also

[Rights Management](uradministration_k_start.htm)

[Dialog Log on to EPLAN](urrightsmanager_d_anmelden.htm)