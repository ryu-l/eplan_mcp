# Graphical and External Components and Connections

The representation type can be used on arbitrary components (that is, [placed functions](Glossary_o_platziertefunktionen.htm)) to specify that the function is graphical or external. The function is then no longer directly reported, that is, it appears neither in message management nor in report lists or [cross-references](Glossary_o_querverweise.htm), etc. This also applies to assigned item information. It is counted as a target, however.

By switching the representation type you can always make a graphical or external function into a functional device. The [properties](Glossary_o_eigenschaften.htm) entered in the function are then shown in [reports](Glossary_o_auswertungen.htm) again.

Graphical and external components are not shown in the device navigator and it is not possible to delete only the placement.

### [ClosedGraphical components](javascript:void(0);)

A graphical [component](Glossary_o_schaltzeichen.htm) can be generated in two ways:

- Place a component on a graphics page.
- Switch the representation type of a function placed on a logical page to "Graphical".

Graphical components on logical pages and components that have been placed on free [graphical pages](Glossary_o_grafikseiten.htm) have exactly the same behavior, that is, there is no difference for the evaluation run.

Graphical components are displayed in a different color. The colors are defined in the user [settings](Glossary_o_einstellungen.htm) (Options > Settings > User > Graphical editing > 2D).

### [ClosedExternal components](javascript:void(0);)

An external component is generated by switching the representation type of a function placed on a logical page to "external".

External components are displayed in a different color. The colors are defined in the user settings (Options > Settings > User > Graphical editing > 2D).

### [ClosedGraphical and external connections](javascript:void(0);)

Connections can also be graphical or external; this can be set on the connection definition point or on the connection. If no connection definition point exists or the connection definition point has the "Not defined" representation type (this is the default setting), then the [connections](Glossary_o_verbindungen.htm) will behave as the [functions](Glossary_o_funktionen.htm) at the end of the connections (the targets) indicate:

- If at least one target of the connection is graphical, then the connection is also graphical.
- If at least one target is external, then the connection is also external. (If it has one graphical and one external target, then the connection is graphical.)
- Only if both targets are logical will the connection be logical (that is, it has the representation type "Single-line" or "Multi-line"). If, however, a connection definition point with the "single-line" or "multi-line" representation type is set between two graphical targets, a logical connection [forms](Glossary_o_formulare.htm) between the two targets that is visible, for instance, in the Connections <Project name> navigator dialog.

Non-placed connections can only be logical.

Placed connections can have one of the following [representation types](Glossary_o_darstellungsarten.htm):

- Not defined (that is, the representation type is determined automatically. This is the default setting for connections and [connection definition points](Glossary_o_verbindungsdefinitionspunkte.htm).)
- Multi-line
- Single-line
- [P&I diagram](Glossary_o_ri_fliessbild.htm)
- Function overview ([fluid power](Glossary_o_fluidtechnik.htm))
- External
- Graphic.

Potentials and PLC [target tracking](Glossary_o_zielverfolgung.htm) are continued via graphical connections.

Graphic and external connections are not designated when numbering connections.

See also

[Behavior of Graphical and External Functions](devicetaggui_k_verhaltengrafischerschaltzeichen.htm)